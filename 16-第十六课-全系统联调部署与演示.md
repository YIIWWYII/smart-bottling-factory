# 第十六课：全系统联调、部署与演示

## 本节学习路线

### 这是什么

这是把所有模块从单独可运行变成一条可重复生产闭环的课程：启动、健康检查、正常瓶、缺陷瓶、异常恢复、真实设备替换和连续彩排。

### 为什么要学

模块分别成功不代表系统成功。只有同一个traceCode能从识别走到入库，状态、数据库、界面、AI、视觉、MQTT和硬件反馈都一致，才算闭环。

### 它在项目中的位置

本节依赖前15节，不能提前跳过。它负责把所有接口、主题、状态、版本、启动命令和备用方案固定下来。

### 需要的软件和部署位置

运行目录统一在E盘的hdc_runtime：MySQL、MQTT、Nginx、后端、AI、视觉、模型、知识库、文件、日志和备份。主服务器、视觉电脑和鸿蒙设备使用固定局域网IP。C盘不放运行数据。

### 最小例子

完全使用模拟器跑一只正常瓶：识别 → AI建议 → 气检 → 初检 → 饮料就绪 → 灌装 → 二检 → 装箱 → AGV → 入库。先证明软件闭环，再逐个替换真实设备。

### 项目实操顺序

1. 冻结HTTP、MQTT、WebSocket、状态和编号协议。
2. 备份数据库、模型和知识库。
3. 启动MySQL。
4. 启动MQTT。
5. 启动AI/RAG。
6. 启动视觉。
7. 启动Spring Boot。
8. 启动Nginx管理端和大屏。
9. 启动鸿蒙工位屏。
10. 启动设备模拟器。
11. 跑正常瓶完整入库。
12. 跑缺陷瓶正确拦截。
13. 单独测试气体、饮料未完成、设备离线、AI停止、视觉停止、WebSocket断线、机械臂失败、AGV避障和仓储报警。
14. 一次只把一个模拟设备替换为真实设备。
15. 连续完整彩排至少5次。

### 每次启动的健康检查

3307可连接、31883可连接、AI健康接口成功、视觉健康接口成功、后端Started、管理端打开、大屏打开、WebSocket OPEN、鸿蒙能拿到工位快照、设备心跳到达。每项检查都记录时间、版本和结果。

### 正常瓶验收操作

固定BOT-DEMO-001、BATCH-DEMO-001、DRINK-DEMO-001、BOX-DEMO-001和AGV-TASK-DEMO-001。每推进一步，同时检查traceCode、MySQL当前状态、process_event、管理端、大屏和鸿蒙屏。最后确认库存增加一次且没有重复命令。

### 缺陷瓶验收操作

使用BOT-DEMO-002，在二检返回CAP_TILTED。预期保存缺陷图片、检测记录和报警，瓶子变为REJECTED或HOLD，不创建装箱任务，不增加仓储库存。

### 如何调试全链路

按traceCode、inspectionId、decisionId、commandId、boxCode、taskCode和alarmId搜索日志和数据库。固定追查顺序：页面请求 → Spring Boot → 数据库 → MQTT发布 → 设备接收 → ACK → WebSocket → 页面处理。一次只定位一段，不同时修改所有模块。

### 如何与项目交付连接

完成本节后，项目具备部署包、启动表、数据库备份、版本清单、正常流程脚本、异常脚本、备用模式和连续演示记录，其他人可以按文档复现。

### 常见错误怎么查

- 端口失败：查启动顺序和端口占用。
- 状态不一致：查事件顺序、版本和事务。
- AI阻塞流程：查超时和审核工艺降级。
- 视觉失败仍放行：查后端是否收到UNKNOWN并阻止。
- 机械臂或AGV失败：任务进入失败或暂停，不能伪造入库。
- 重启后重复动作：查未完成command和设备重连策略。
- 大屏旧数据：查WebSocket重连后的快照。

### 如何与项目交付连接

完成本节后，项目具备部署包、启动表、数据库备份、版本清单、正常流程脚本、异常脚本、备用模式和连续演示记录，其他人可以按文档复现。

### 通过门禁

断电重启能按启动表恢复；正常瓶连续5次入库；缺陷瓶连续5次拦截；AI和视觉失败有降级；MQTT和WebSocket断线可恢复；所有关键操作可追溯；软件、模型、鸿蒙和数据库版本一致。

## 学习目标

> 重要边界：本课的完成必须以实际运行和连续验收为准。当前仓库已经验证的是两个 Vue 前端构建和现有 Spring Boot 编译；新产线状态机、鸿蒙、视觉、AI/RAG/MCP、机械臂、AGV 和仓储业务仍需按第 1 至第 15 课逐步实现，不能把课程文档当成已经完成的功能。

- 冻结HTTP、MQTT、WebSocket和状态枚举协议。
- 先用模拟设备跑通，再逐个替换真实硬件。
- 在E盘完成可重复启动的部署。
- 测试正常、异常、断网和降级流程。
- 形成稳定、可追溯、可连续演示的完整产线。

## 最终结构

```text
传感器、PLC、机械臂、AGV
↔ MQTT Broker
↔ Spring Boot
↔ MySQL
↔ AI/RAG/MCP与视觉服务
→ 管理端、数字大屏、鸿蒙工位屏
```

通信边界：

- 设备与后端：MQTT。
- 视觉与后端：HTTP。
- AI与后端：HTTP和受控MCP工具。
- 界面查询与操作：HTTP。
- 后端向界面实时推送：WebSocket。

## 联调前冻结四份协议

### HTTP接口表

- 路径与方法。
- 请求和响应字段。
- 状态码、权限和超时。
- 幂等要求。

### MQTT主题表

- 主题、发布方和订阅方。
- QoS、JSON结构和频率。
- 是否保留。

### WebSocket事件表

- 事件类型、触发条件和接收界面。
- `data`结构、版本和去重字段。

### 状态与枚举表

```text
RUNNING统一表示运行中
FAULT统一表示故障
VOC统一ppm
速度统一m/s
时间统一ISO 8601
```

不能后端用`RUNNING`、鸿蒙用`WORKING`、大屏用`1`。

## 统一追踪编号

```text
batchCode       生产批次
traceCode       单瓶追踪号
inspectionId    检测编号
decisionId      AI决策编号
commandId       设备命令编号
boxCode         箱子编号
taskCode        AGV任务编号
alarmId         报警编号
```

示例关联：

```text
BOT-0001
→ AI-DEC-001
→ CMD-0012
→ VIS-0008
→ BOX-001
→ AGV-TASK-001
```

## 联调阶段

### 纯软件闭环

- 模拟瓶型识别。
- 模拟传感器。
- 模拟设备ACK。
- 模拟机械臂、AGV和仓储。
- 跑通从识别到入库的完整数据链。

### 单工位真实接入

建议先接气体传感器：

```text
真实上报
→ MQTT
→ 后端
→ 数据库
→ 报警
→ 大屏和鸿蒙屏
```

### 关键设备逐个替换

```text
摄像头
→ 传送带
→ 机械臂
→ AGV
→ 灌装设备
```

一次只替换一个，始终保留模拟模式。

### 混合完整流程

- 已接设备使用`REAL`。
- 未完成设备使用`SIMULATION`。
- 管理端和大屏明确显示模式。

## 模拟器要求

- 正常运行。
- 气体超限。
- 视觉缺陷。
- 设备离线。
- 命令失败和超时。
- AGV遇障。
- 仓储安全报警。

模拟器是开发和故障演示工具，不是隐瞒未完成硬件。

## 部署机器

### 主服务器

- MySQL。
- MQTT Broker。
- Spring Boot。
- AI/RAG/MCP。
- Nginx。
- 管理端和数字大屏。

### 视觉电脑

- 摄像头。
- OpenCV和模型。
- FastAPI视觉服务。

电脑性能足够时可与主服务器合并。

### 鸿蒙设备

- 安装同一ArkTS应用。
- 不同设备绑定不同`stationCode`。

### 控制器

- 传感器采集。
- 本地设备动作和安全逻辑。
- MQTT通信。

## 端口规划

```text
管理端       8080
数字大屏     8081
Spring Boot  8088
MySQL        3307
MQTT         31883
AI服务       8090（建议）
视觉服务     8091（建议）
```

确定后统一配置，不允许各模块随意修改。

## 局域网规划

```text
主服务器：192.168.10.10
视觉电脑：192.168.10.11
水洗工位屏：192.168.10.21
检测工位屏：192.168.10.22
AGV控制器：192.168.10.31
```

连接地址：

```text
Spring Boot：http://192.168.10.10:8088
MQTT：tcp://192.168.10.10:31883
AI：http://192.168.10.10:8090
视觉：http://192.168.10.11:8091
```

演示前检查：

- 同一网段。
- IP固定。
- 防火墙允许端口。
- 关闭不必要网络切换。
- 电脑时钟一致。
- 不依赖现场互联网。

## 启动顺序

```text
1. MySQL
2. MQTT Broker
3. AI/RAG服务
4. 视觉服务
5. Spring Boot
6. Nginx管理端和大屏
7. 鸿蒙工位应用
8. 真实设备控制器或模拟器
```

健康检查：

- MySQL能查询。
- MQTT可发布订阅。
- AI健康接口正常。
- 视觉可识别测试图片。
- Spring Boot接口正常。
- WebSocket连接正常。
- 鸿蒙屏能查询工位。
- 设备心跳到达。

## E盘部署目录

```text
E:\hdc_runtime
├─ mysql
├─ nginx
├─ mqtt
├─ backend
├─ ai
├─ vision
├─ models
├─ knowledge
├─ files
├─ logs
└─ backup
```

- Python模型、DevEco SDK、Maven仓库和数据集不放C盘。
- 日志设置滚动和保留天数，避免E盘也被写满。

## 构建产物

### Vue

```powershell
npm run build
```

静态文件交给Nginx。

### Spring Boot

```powershell
mvn clean package
```

生成JAR并由Java运行。

### 鸿蒙

- DevEco Studio构建HAP。
- 安装到工位设备。

### 版本记录

- Web版本。
- 后端版本。
- AI和提示词版本。
- 视觉模型版本。
- 鸿蒙HAP版本。
- 数据库脚本版本。

## 标准测试场景

### 正常

- 正常瓶完整入库。
- 两条并行支线正确汇合。
- 机械臂、AGV和仓储记录一致。

### 质量和安全

- 气体超限：禁止下一工序并报警。
- 初检缺陷：剔除并记录。
- 饮料批次未完成：禁止灌装。
- 二检瓶盖歪斜：剔除并保存图片。
- 仓储气体或烟雾报警：触发安全流程。

### 设备与网络

- 机械臂失败：装箱任务失败并报警。
- AGV遇障：本地停车并上报。
- MQTT断开：设备离线并禁止控制。
- WebSocket断开：界面标记过期并重连。
- 重复消息：不重复库存和命令。

### AI

- AI不可用：使用已审核工艺或人工确认。
- RAG无证据：`NEED_REVIEW`。
- 越界建议：后端拒绝。

## 日志与定位

日志包含：

```text
时间
模块
requestId
traceCode
commandId
设备编号
事件类型
处理结果
错误原因
```

固定排查：

```text
页面是否发请求
→ Spring Boot是否收到
→ 数据库是否更新
→ MQTT是否发布
→ 设备是否收到
→ ACK是否返回
→ WebSocket是否推送
→ 页面是否处理
```

一次定位一段，不同时修改五个模块。

## 比赛演示脚本

### 系统启动

- 设备在线。
- 产线空闲。
- 当前瓶型与批次。
- AI、视觉、MQTT和WebSocket正常。

### 瓶型与AI决策

```text
摄像头识别PLA-500
→ RAG查询工艺
→ MCP查询数据库
→ AI参数建议
→ 后端校验
→ 工位参数更新
```

管理端展示知识来源和决策记录。

### 两条并行支线

- 瓶子静置、水洗、风洗、气体检测。
- 饮料调配、温湿度和杀菌。
- 两边合格后进入灌装。

### 灌装与二检

- 完成简化灌装和封盖。
- 展示瓶盖、瓶身、瓶底和液位结果。

### 装箱运输入库

```text
计算摆放位置
→ 机械臂装箱
→ 创建AGV任务
→ AGV运输和避障
→ 仓储入库
```

### 异常展示

- 主流程使用一只正常瓶完整走到底。
- 第二轮放入预制缺陷瓶。
- 视觉识别瓶盖歪斜。
- 后端禁止放行、剔除并展示图片和报警。

## 备用方案

- 摄像头失败：本地预拍图片。
- AI失败：缓存决策并明确降级。
- 机械臂失败：切换模拟任务和坐标展示。
- AGV失败：固定路线或模拟任务。
- 灌装不稳定：小流量泵或模拟灌装。
- 封盖失败：压盖或预放瓶盖。
- 网络异常：自带路由器和本地局域网。
- 大屏故障：准备录屏作为最后备用。

备用不替代主流程，但必须提前验证。

## 四人联调分工

### 学生1

- Spring Boot、MySQL、MQTT。
- 设备模拟器、命令和ACK。

### 学生2

- 管理端、数字大屏、WebSocket展示。

### 学生3

- 鸿蒙工位屏、HTTP、WebSocket、报警与操作。

### 负责人

- AI、RAG、MCP和视觉统筹。
- 接口协调、完整流程验收和演示组织。

每日短会回答：昨天完成、今天接口、当前阻塞、是否影响主流程。

## 最终完成标准

- 断电重启后能按文档启动。
- 每个服务有健康检查。
- 正常瓶完整入库。
- 缺陷瓶被识别和剔除。
- AI有知识引用和数据库校验。
- 鸿蒙屏显示设备最终结果。
- 管理端和大屏实时一致。
- 关键操作可追溯。
- 设备离线、AI失败和网络异常有降级方案。
- 完整演示连续成功至少五次。

## 最终实施顺序

```text
接口协议
→ 模拟闭环
→ 单设备真实接入
→ 逐个替换硬件
→ 异常测试
→ 连续彩排
```

## 具体实操：完成一次从零启动到连续演示

### 步骤1：制作启动检查表

在：

```text
E:\hdc_runtime\启动检查表.md
```

写下：

```text
MySQL端口：3307
MQTT端口：31883
后端端口：8088
管理端端口：8080
大屏端口：8081
AI端口：8090
视觉端口：8091
主服务器IP：________
```

每项增加“启动命令、成功标志、失败处理”。

### 步骤2：导出开发数据库备份

在数据库工具中导出开发库结构和演示数据，保存到：

```text
E:\hdc_runtime\backup\hdc-demo.sql
```

恢复前确认目标数据库，不要把演示数据导入生产或重要数据库。

### 步骤3：启动基础服务

按顺序启动并逐项打勾：

```text
MySQL
MQTT Broker
AI/RAG
视觉服务
Spring Boot
管理端和大屏
鸿蒙工位屏
设备模拟器或控制器
```

每启动一个服务马上做健康检查，不要全部启动后才排错。

### 步骤4：执行健康检查

```powershell
Test-NetConnection localhost -Port 3307
Test-NetConnection localhost -Port 31883
Test-NetConnection localhost -Port 8088
Test-NetConnection localhost -Port 8080
Test-NetConnection localhost -Port 8081
Invoke-RestMethod http://localhost:8090/health
Invoke-RestMethod http://localhost:8091/health
```

检查结果写入启动表。视觉端还要实际打开测试图片，AI端还要实际完成一次测试决策。

### 步骤5：准备演示数据

固定：

```text
批次：BATCH-DEMO-001
正常瓶：BOT-DEMO-001
缺陷瓶：BOT-DEMO-002
瓶型：PLA-500
饮料批次：DRINK-DEMO-001
箱子：BOX-DEMO-001
AGV任务：TASK-DEMO-001
```

清理上一次演示的临时状态，但保留必要的知识库、工艺版本和设备基础数据。

### 步骤6：先跑模拟正常流程

按顺序执行并在每一步确认数据库和界面：

1. 发布瓶型识别事件。
2. 查看AI决策和工艺建议。
3. 后端校验并加载参数。
4. 模拟静置、清洗和风洗完成。
5. 发布气体合格值`6.2 ppm`。
6. 发布初检`PASS`。
7. 发布饮料批次`READY_FOR_FILLING`。
8. 提交灌装和封盖命令。
9. 发布二检`PASS`。
10. 生成装箱坐标并模拟机械臂成功。
11. 创建AGV任务并模拟到达。
12. 完成仓储入库。

每一步检查：

```text
traceCode是否不变
数据库状态是否正确
管理端是否更新
大屏是否更新
鸿蒙屏是否更新
```

### 步骤7：跑缺陷流程

使用`BOT-DEMO-002`：

1. 识别为`PLA-500`。
2. 走到二次检测。
3. 视觉返回`CAP_TILTED`。
4. Spring Boot保存图片和检测结果。
5. 状态变为`REJECTED`或`HOLD`。
6. 禁止装箱。
7. 页面显示缺陷原因和报警。

确认这个瓶子不能出现在入库库存中。

### 步骤8：逐项测试异常

每次只制造一个异常：

```text
气体超限
饮料批次未完成
设备离线
MQTT断开
视觉服务停止
AI服务停止
WebSocket断开
机械臂失败
AGV遇障
仓储烟雾报警
```

每项记录：

```text
触发方法
预期状态
实际状态
页面提示
恢复方法
```

### 步骤9：测试重启恢复

1. 正常流程运行到中途。
2. 停止Spring Boot。
3. 重新启动。
4. 检查数据库状态是否保留。
5. 检查设备心跳是否恢复。
6. 检查WebSocket页面是否重连并加载快照。
7. 检查未完成命令不会被重复发送。

### 步骤10：制作版本清单

```text
Web管理端版本：
数字大屏版本：
Spring Boot JAR版本：
AI代码与模型版本：
视觉模型版本：
鸿蒙HAP版本：
数据库脚本版本：
MQTT协议版本：
```

所有演示电脑和设备使用同一份清单。

### 步骤11：连续彩排

按照真实演示脚本连续成功5次，每次记录耗时和问题。任何一次失败都要归类：

```text
环境问题
数据问题
网络问题
后端问题
硬件问题
演示操作问题
```

不要只在成功一次后认为系统完成。

### 步骤12：准备切换备用方案

为每个高风险设备准备明确切换按钮或配置：

```text
cameraMode=REAL/SIMULATION
agvMode=REAL/SIMULATION
armMode=REAL/SIMULATION
aiMode=LIVE/CACHED
visionMode=CAMERA/IMAGE
```

界面显示当前模式，不能让评委误以为真实设备在执行模拟动作。

### 本课完成标志

- 有一份从启动到关闭的操作手册。
- 正常瓶连续5次完整入库。
- 缺陷瓶连续5次正确拦截。
- 每个服务和端口都有健康检查。
- 断网、服务停止和设备失败都有恢复步骤。
- 所有演示设备、软件和数据版本一致。

## 保姆式部署：第一次从零启动

### 第 1 步：把 Docker 数据放到 E 盘

如果使用 Docker 运行 MySQL，先在 Docker Desktop Settings 中把磁盘镜像或数据目录迁移到 E 盘，例如 E:\Docker\data。迁移完成后再拉取镜像；只把容器卷放到 E 盘而让 Docker 镜像层留在 C 盘，仍然可能把 C 盘写满。

### 第 2 步：启动 MySQL

~~~powershell
New-Item -ItemType Directory -Force 'E:\hdc_runtime\mysql\data' | Out-Null
docker rm -f hdc-mysql 2>$null
docker run --name hdc-mysql -e MYSQL_ROOT_PASSWORD=123456 -e MYSQL_DATABASE=hdc -p 3307:3306 -v 'E:\hdc_runtime\mysql\data:/var/lib/mysql' -d mysql:8.0
docker logs -f hdc-mysql
~~~

看到 MySQL ready 后按 Ctrl+C 退出日志查看，不要停止容器。验证：

~~~powershell
Test-NetConnection 127.0.0.1 -Port 3307
~~~

初始化脚本来自 数字大屏后端code\hdc_server\src\main\resources\sql。先备份数据库，再根据脚本说明执行，不要把不确定的 SQL 重复执行到正式演示库。

### 第 3 步：启动 MQTT

~~~powershell
Set-Location 'E:\2026挑战杯\工业沙盘2026\数字展板前后端\.runtime\mqtt-broker'
node .\start-broker.js
~~~

另开窗口确认：

~~~powershell
Test-NetConnection 127.0.0.1 -Port 31883
~~~

### 第 4 步：编译并启动 Spring Boot

~~~powershell
Set-Location 'E:\2026挑战杯\工业沙盘2026\数字展板前后端\数字大屏后端code\hdc_server'
$mvn='E:\dev\apache-maven-3.9.9\bin\mvn.cmd'
& $mvn '-Dmaven.repo.local=E:/dev/maven-repository' '-DskipTests' 'package'
java -jar '.\target\hdc-2024031501.jar'
~~~

必须看到 Started HdcApplication。如果报数据库或 MQTT 错误，先检查 3307 和 31883，不要修改前端地址。

### 第 5 步：启动两个前端

管理端：

~~~powershell
Set-Location 'E:\2026挑战杯\工业沙盘2026\数字展板前后端\数字大屏前端code\hdc_web'
npm run serve
~~~

打开 http://localhost:8080/#/factory。

大屏端另开窗口：

~~~powershell
Set-Location 'E:\2026挑战杯\工业沙盘2026\数字展板前后端\数字大屏前端code\hdc_digitallargescreen_web'
npm run serve -- --port 8081
~~~

打开 http://localhost:8081/#/factoryOverview。旧大屏仍可通过 http://localhost:8081/#/dataOverview 访问。前端接口地址在 src/api/Service.js，WebSocket 地址在 src/api/WebSocket.js，修改后要重启对应前端。

### 第 6 步：基础健康检查

~~~powershell
Test-NetConnection 127.0.0.1 -Port 3307
Test-NetConnection 127.0.0.1 -Port 31883
Test-NetConnection 127.0.0.1 -Port 8088
Test-NetConnection 127.0.0.1 -Port 8080
Test-NetConnection 127.0.0.1 -Port 8081
~~~

五个端口都通过后，再检查管理端、大屏、后端接口和 WebSocket。任何一项失败就停止联调，先修复当前服务。

### 第 7 步：最终阶段门禁

~~~text
门禁1：环境和 E 盘目录通过
门禁2：现有两个前端构建通过
门禁3：现有后端编译并启动通过
门禁4：数据库追溯和状态机测试通过
门禁5：MQTT 正常、超限、ACK、超时通过
门禁6：WebSocket 管理端和大屏实时更新通过
门禁7：鸿蒙工位查询和控制闭环通过
门禁8：视觉正常、缺陷、UNKNOWN、相机离线通过
门禁9：AI 有证据、后端校验、人工确认和回退通过
门禁10：正常瓶和缺陷瓶连续各 5 次通过
~~~

未通过当前门禁时，禁止继续增加下一类功能。回退到最近一个通过门禁的版本，记录错误、日志和复现步骤。

### 第 8 步：演示模式必须显式标注

没有真实设备的模块使用 SIMULATION，真实设备使用 REAL。视觉固定图片、AI 缓存决策和模拟机械臂/AGV 都必须在界面显示当前模式，不能让评委误以为模拟器正在驱动真实设备。

### 第 9 步：最终判定

只有当一条固定 traceCode 能从瓶型识别开始，经过气体合格、外观检测、灌装条件、二检、装箱、AGV 任务和仓储入库，并且管理端、大屏、鸿蒙屏、数据库、日志中的编号一致，才称为系统闭环。课程文件不是完成证明，连续可复现实验才是完成证明。
