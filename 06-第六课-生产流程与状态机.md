# 第六课：生产流程与状态机

## 本节学习路线

### 这是什么

状态机是后端用来描述瓶子、饮料、设备、工位和命令当前处于什么状态，以及什么事件可以让它们进入下一状态的规则系统。

### 为什么要学

没有状态机，流程会变成大量嵌套判断，容易出现未气检就灌装、二检失败仍装箱、重复事件重复入库等逻辑错误。

### 它在项目中的位置

状态机位于Spring Boot业务Service层，数据库保存当前状态和历史事件，MQTT和视觉只提供事件，AI不能绕过状态机放行。

### 需要的软件

- IntelliJ IDEA：写状态转换器和断点。
- JUnit或IDE测试运行器：测试转换。
- MySQL客户端：查看bottle和process_event。
- Postman或PowerShell：提交设备事件。
- draw.io或Mermaid：画转换表。

### 最小例子

当前GAS_INSPECTION/RUNNING，收到GAS_RESULT且VOC=6.2ppm，阈值10ppm，结果必须是FIRST_INSPECTION/WAITING。把值改为12.5ppm，结果必须是GAS_INSPECTION/HOLD。

### 项目实操顺序

1. 冻结currentStep、status、deviceState和commandState枚举。
2. 写状态转换表。
3. 先写不查数据库、不发MQTT的纯转换器。
4. 给合法和非法转换写测试。
5. 在Service中查询瓶子当前状态。
6. 调用纯转换器。
7. 在事务中更新bottle和process_event。
8. 处理eventId幂等。
9. 实现瓶子与饮料批次汇合。
10. 实现HOLD、人工复核和设备离线。

### 状态机调试怎么做

在转换入口打断点，观察currentStep、currentStatus、eventType、eventId、eventValue、threshold和transitionResult。每处理一个事件，都查询数据库当前状态和历史事件。页面颜色不对时先查数据库，不先改前端。

### 常见错误怎么查

- 跳步成功：检查转换表和当前状态查询是否执行。
- 重复事件重复处理：检查eventId唯一索引和先查重复逻辑。
- 气体超限却放行：检查单位、阈值来源和比较方向。
- 灌装提前开始：检查瓶子、饮料、设备和报警四组条件。
- 设备离线仍继续：检查设备状态是否参与Guard。
- 当前状态对但历史缺：检查事务是否同时提交。

### 如何与下一节连接

第7课把MQTT遥测和ACK转换成状态机事件；第8课把状态机变化推送给页面；第13课鸿蒙按钮只提交命令，是否允许执行仍由本节规则决定。

### 通过门禁

正常、超限、跳步、重复、饮料未完成、设备离线和人工恢复全部有测试，且当前状态与历史事件一致，才能接真实设备。

## 学习目标

- 理解状态、事件、条件和动作。
- 为瓶子、饮料批次、工位、设备和命令分别设计状态。
- 实现瓶子与饮料两条并行流程的灌装汇合条件。
- 拒绝跳步、重复事件和非法转换。
- 设计暂停、异常、返工、人工处理和AI降级。

## 为什么需要状态机

大量嵌套判断会变成：

```java
if (气体合格) {
    if (外观合格) {
        if (饮料准备完成) {
            if (灌装机在线) {
                // 灌装
            }
        }
    }
}
```

工序增加后，暂停、恢复、返工和离线很难维护。

状态机表达：

```text
当前状态 + 发生事件 + 条件满足
→ 新状态 + 执行动作
```

示例：

```text
当前：等待气体检测
事件：收到检测结果
条件：浓度低于安全上限
结果：气体检测通过，允许外观检测
```

## 四个概念

- 状态`State`：当前处于什么阶段。
- 事件`Event`：发生了什么。
- 条件`Guard`：是否允许转换。
- 动作`Action`：转换后保存、报警、通知或下发什么。

状态机由真实或模拟事件推动，不是定时播放动画。

## 不使用巨型总状态

分别维护：

- 瓶子状态。
- 饮料批次状态。
- 工位状态。
- 设备状态。
- 控制命令状态。
- 装箱状态。
- AGV任务状态。

需要时由后端组合判断，不创建“瓶子水洗完成但饮料杀菌中且AGV故障”这种巨大状态。

## 瓶子状态

### 当前工序`current_step`

```text
IDENTIFICATION       瓶型识别
PRETREATMENT         静置、清洗和风洗
GAS_INSPECTION       气体检测
FIRST_INSPECTION     第一次外观检测
WAITING_FILLING      等待灌装
FILLING              灌装封盖
SECOND_INSPECTION    二次检测
PACKING              装箱
TRANSPORTING         运输
WAREHOUSING          入库
COMPLETED            全流程完成
```

### 当前执行状态`status`

```text
WAITING     等待
RUNNING     执行中
PASSED      合格
FAILED      不合格
HOLD        暂停等待处理
REJECTED    已剔除
```

示例：

```text
current_step = GAS_INSPECTION
status = FAILED
```

## 瓶子流程

```text
创建追踪记录
→ 瓶型识别
→ 预处理
→ 气体检测
→ 第一次外观检测
→ 等待灌装
→ 灌装和封盖
→ 二次检测
→ 装箱
→ AGV运输
→ 仓储入库
→ 完成
```

异常分支：

- 无法识别：进入人工确认。
- 气体不合格：HOLD或REJECTED。
- 初检或二检不合格：剔除或人工复核。
- 设备故障：暂停当前工序。

非法事件示例：

```text
当前工序：GAS_INSPECTION
收到事件：FILLING_COMPLETED
```

后端必须拒绝、保留原状态并记录异常，不能直接跳步。

## 饮料批次状态

```text
CREATED
→ MIXING
→ TEMPERATURE_STABILIZING
→ STERILIZING
→ QUALITY_CHECK
→ READY_FOR_FILLING
```

异常状态：

```text
HOLD
FAILED
DISCARDED
```

## 灌装汇合条件

```java
boolean fillingAllowed =
    bottle.getCurrentStep() == WAITING_FILLING
    && bottle.getStatus() == PASSED
    && beverageBatch.getStatus() == READY_FOR_FILLING
    && fillingStation.getState() == READY
    && fillingDevice.isOnline()
    && !hasActiveSafetyAlarm();
```

瓶子支线与饮料支线必须都合格，才能汇合灌装。

## 工位和设备状态

### 工位状态

```text
OFFLINE       离线
IDLE          空闲
READY         已准备
RUNNING       运行中
PAUSED        暂停
FAULT         故障
MAINTENANCE   维修中
```

### 运行模式

```text
AUTO          自动模式
MANUAL        手动模式
MAINTENANCE   维修模式
```

状态与模式分开。例如`state=IDLE`、`mode=MANUAL`表示设备正常空闲，等待人工控制。

## 控制命令状态

```text
CREATED
→ VALIDATED
→ SENT
→ ACKED
→ SUCCESS
```

异常：

```text
REJECTED   后端校验不通过，未发送
FAILED     设备收到但执行失败
TIMEOUT    未及时收到设备反馈
```

## 事件格式

```json
{
  "eventId": "EVT-20260719-0001",
  "eventType": "GAS_INSPECTION_COMPLETED",
  "bottleCode": "BOT-000001",
  "stationCode": "GAS-01",
  "occurredAt": "2026-07-19T10:20:30+08:00",
  "data": {
    "concentration": 6.2,
    "unit": "ppm",
    "threshold": 10.0
  }
}
```

处理顺序：

1. 检查`eventId`是否已处理。
2. 查询瓶子当前状态。
3. 检查事件是否适用于当前工序。
4. 校验数值、单位和安全阈值。
5. 保存检测记录。
6. 更新瓶子工序和状态。
7. 生成下一工位任务。
8. WebSocket推送最新状态。

## 幂等性

- MQTT可能因重试重复发送消息。
- 每个事件使用唯一`eventId`。
- 每个命令使用唯一`commandId`。
- 已处理的相同编号直接返回原结果。
- 重复“装箱完成”不能让库存增加两次。

## 当前状态与历史事件

### `bottle`表回答当前在哪里

```text
trace_code
batch_code
current_step
status
current_station
version
update_time
```

### `process_event`表回答经历过什么

```text
event_id
bottle_id
from_step
from_status
event_type
to_step
to_status
station_code
result_data
occurred_at
```

两者都需要，不能只保存最终状态。

## 异常处理

### 气体不合格

- 瓶子进入`HOLD`或`REJECTED`。
- 创建安全报警。
- 禁止进入灌装。
- 每次复检都保存独立记录。

### 设备中途离线

- 工位进入`FAULT`。
- 当前工序进入`HOLD`。
- 控制命令进入`TIMEOUT`。
- 通知工位屏和大屏。
- 恢复后先确认真实状态，不自动继续动作。

### AI不可用

- 停止AI自动调整。
- 继续使用已审核工艺版本。
- 允许人工确认。
- 安全规则继续运行。

### 人工处理

允许有权限人员返工、剔除或处理异常，并记录：

```text
操作人
操作时间
原状态
新状态
原因
关联报警
```

安全检测失败不向普通用户提供强制放行。

## 鸿蒙工位屏与状态机

后端返回允许操作：

```json
{
  "stationCode": "WASH-01",
  "state": "IDLE",
  "mode": "MANUAL",
  "currentStep": "PRETREATMENT",
  "allowedActions": [
    "START_WASH",
    "REJECT_BOTTLE"
  ]
}
```

- 鸿蒙屏只启用`allowedActions`中的按钮。
- 隐藏按钮不能代替后端校验。
- 后端收到请求后仍需再次检查。

## AI与状态机边界

AI适合回答：

- PLA-500建议水洗多久。
- 缺陷可能由什么原因造成。
- 建议传送带速度是多少。

状态机负责回答：

- 当前是否允许水洗。
- 气体未完成能否灌装。
- 设备故障时能否继续。
- 瓶子是否已装箱。

## 实操作业

- 画出瓶子和饮料批次两条状态机。
- 写出灌装汇合条件。
- 定义瓶子、工位、设备和命令状态。
- 列出每个工序允许接收的事件。
- 设计气体合格、不合格和设备离线三条转换。
- 用Spring Service实现前三个瓶子工序。
- 测试正常转换、跳步、重复事件和未知瓶子。
- 让接口返回`allowedActions`。

## 验收问题

### 问题

瓶子仍在气体检测工位，后端收到“二次检测合格”事件怎么办？

### 答案

拒绝非法转换，保留原状态并记录异常事件，不能直接跳到装箱阶段。

## 具体实操：用纸面规则实现第一版状态机

### 步骤1：先写状态字典

新建`状态字典.md`，逐行写：

```text
IDENTIFICATION：瓶型识别
PRETREATMENT：静置、清洗和风洗
GAS_INSPECTION：气体检测
FIRST_INSPECTION：第一次外观检测
WAITING_FILLING：等待灌装
FILLING：灌装和封盖
SECOND_INSPECTION：二次检测
PACKING：装箱
TRANSPORTING：运输
WAREHOUSING：入库
COMPLETED：完成
```

另写状态：

```text
WAITING、RUNNING、PASSED、FAILED、HOLD、REJECTED
```

### 步骤2：做转换表

按下面格式填写，不允许只画箭头：

```text
当前工序 | 当前状态 | 事件 | 条件 | 下一工序 | 下一状态 | 动作
```

先填写五行：

```text
IDENTIFICATION | RUNNING | BOTTLE_IDENTIFIED | 置信度>=0.8 | PRETREATMENT | WAITING | 创建预处理任务
PRETREATMENT | RUNNING | PRETREATMENT_DONE | 设备成功 | GAS_INSPECTION | WAITING | 请求气体检测
GAS_INSPECTION | RUNNING | GAS_RESULT | VOC<阈值 | FIRST_INSPECTION | WAITING | 触发视觉初检
GAS_INSPECTION | RUNNING | GAS_RESULT | VOC>=阈值 | GAS_INSPECTION | HOLD | 创建报警
FIRST_INSPECTION | RUNNING | INSPECTION_DONE | result=PASS | WAITING_FILLING | WAITING | 等待饮料批次
```

### 步骤3：实现转换函数

先不接数据库，在Service中写一个纯函数：

```java
public TransitionResult handleEvent(
        String step, String status, String eventType,
        BigDecimal value) {
    if ("GAS_INSPECTION".equals(step)
            && "RUNNING".equals(status)
            && "GAS_RESULT".equals(eventType)) {
        if (value.compareTo(new BigDecimal("10")) < 0) {
            return new TransitionResult(
                    "FIRST_INSPECTION", "WAITING", "GAS_PASS");
        }
        return new TransitionResult(
                "GAS_INSPECTION", "HOLD", "GAS_OVER_LIMIT");
    }
    return new TransitionResult(step, status, "ILLEGAL_TRANSITION");
}
```

### 步骤4：写四个单元测试场景

输入与预期：

```text
GAS_INSPECTION/RUNNING/GAS_RESULT/6.2 → FIRST_INSPECTION/WAITING
GAS_INSPECTION/RUNNING/GAS_RESULT/12.5 → GAS_INSPECTION/HOLD
GAS_INSPECTION/RUNNING/FILLING_DONE/无 → 原状态不变
GAS_INSPECTION/RUNNING/GAS_RESULT/相同eventId → 不重复处理
```

没有测试框架时，也可以先写一个`main`逐条打印，之后再改为JUnit。

### 步骤5：加入饮料批次汇合

准备两组状态：

```text
瓶子：WAITING_FILLING/PASSED
饮料批次：READY_FOR_FILLING
```

调用`canStartFilling()`，预期为`true`。

再逐个改成：

```text
瓶子未通过
饮料批次MIXING
灌装设备OFFLINE
存在CRITICAL报警
```

每次预期都为`false`。

### 步骤6：测试跳步

提交以下事件：

```json
{
  "eventId": "EVT-TEST-001",
  "eventType": "SECOND_INSPECTION_COMPLETED",
  "bottleCode": "BOT-TEST-001",
  "currentStep": "GAS_INSPECTION"
}
```

预期：

```text
HTTP业务失败或事件状态REJECTED
瓶子仍为GAS_INSPECTION
新增非法事件日志
```

### 步骤7：测试重复事件

连续发送两次完全相同的`eventId`：

```text
第一次：更新状态并保存事件
第二次：返回第一次处理结果，不重复更新
```

数据库给`event_id`加唯一约束，Service捕获重复后返回原结果。

### 步骤8：设计人工处理

只允许授权用户执行：

```text
HOLD → MANUAL_REVIEW
MANUAL_REVIEW → REJECTED
MANUAL_REVIEW → REWORK
```

强制放行不能作为普通按钮，必须记录用户、原因、报警和时间。

### 本课完成标志

- 有完整转换表。
- 正常、超限、跳步和重复事件都有测试。
- 瓶子与饮料汇合条件能被程序判断。
- 设备故障会让工序暂停，不会直接完成。
