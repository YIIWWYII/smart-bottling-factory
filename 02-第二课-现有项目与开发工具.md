# 第二课：现有项目、开发工具与故障定位

## 本节学习路线

### 这是什么

这是开发环境和现有项目的入门课：先安装和验证工具，再启动现有管理端、大屏、后端、MySQL和MQTT。

### 为什么要学

零基础团队最常见的问题不是不会写代码，而是目录错、版本错、端口错、服务没启动和不知道看哪里。环境不稳定，后面每个模块都会被假故障拖住。

### 它在项目中的位置

第2课是所有人共享的起点。后面的接口、数据库、后端和前端调试都默认你们能独立启动项目、查看日志和判断端口。

### 最小例子

只启动管理端并打开http://localhost:8080，再在页面点击一个按钮，用浏览器Network找到请求。这个例子证明代码、浏览器和开发工具已经连起来。

### 软件安装顺序

1. JDK 8。
2. Maven及E盘本地仓库。
3. Node.js和npm及E盘缓存。
4. Git。
5. IntelliJ IDEA或Eclipse。
6. VS Code。
7. MySQL客户端。
8. MQTTX或MQTT Explorer。
9. 浏览器Chrome或Edge。

安装目录、SDK、依赖缓存和下载包统一放E盘。每个软件安装后立刻执行版本命令并记录，不要一次装完再排错。

### 最小例子

只启动管理端并打开http://localhost:8080，再在页面点击一个按钮，用浏览器Network找到请求。这个例子证明代码、浏览器和开发工具已经连起来。

### 项目实操顺序

1. 复制原始项目作为开发副本。
2. 检查package.json、pom.xml和配置文件。
3. 启动MySQL。
4. 启动MQTT Broker。
5. 启动Spring Boot。
6. 启动管理端和大屏。
7. 用端口检查命令逐项验证。
8. 初始化Git并提交可运行基线。
9. 故意停止服务，练习定位。

### 如何调试

- 页面打不开：先查前端端口。
- 页面有但没有数据：看Network请求。
- 请求404：查上下文路径和Controller映射。
- 请求500：看后端第一个异常。
- 后端数据库失败：查3307和用户名密码。
- 实时不更新：看Network→WS。
- 后端收不到设备消息：看MQTT Broker和订阅主题。

### 如何与下一节连接

第3课会使用本节已经启动的后端和浏览器工具抓取HTTP请求；第4课会使用本节确认的MySQL连接；第7课会使用本节的MQTT Broker。

### 通过门禁

四个人必须都能独立启动、停止和恢复现有项目，并能在五分钟内判断一个服务故障属于哪个层。

## 学习目标

- 找到两个Vue前端和Spring Boot后端的入口。
- 理解现有技术版本与目录职责。
- 独立启动、停止和检查五个运行服务。
- 能定位问题属于前端、后端、数据库、MQTT还是WebSocket。
- 建立四人协作需要的Git规则。

## 当前项目组成

```text
数字大屏前端code
├─ hdc_web                      Web管理端
└─ hdc_digitallargescreen_web   Web数字大屏

数字大屏后端code
└─ hdc_server                   Spring Boot后端
```

- 当前还没有鸿蒙ArkTS工程。
- 当前还没有Python AI与视觉工程。
- 项目根目录当前不是Git仓库，正式修改前必须建立版本管理。

## 当前技术栈

### 管理端

- Vue 2.6。
- Vue Router 3。
- Element UI 2。
- Vue CLI 3。
- Axios负责HTTP。
- 浏览器WebSocket负责实时状态。

### 数字大屏

- 与管理端相同的Vue基础。
- ECharts绘制图表。
- MQTT前端依赖和WebSocket实时数据。
- Vue Grid Layout和固定设计尺寸支持大屏布局。

### 后端

- Java 8。
- Spring Boot 2.x。
- MySQL 8驱动。
- MyBatis Plus访问数据库。
- Eclipse Paho连接MQTT。
- Spring WebSocket。
- Knife4j接口文档。

### 版本策略

- 第一阶段不要升级Vue、Java或Spring Boot。
- 当前Spring依赖存在版本混用，贸然升级可能引发兼容问题。
- 先在现有版本完成业务闭环，再单独安排升级。

## 管理端目录

路径：`数字大屏前端code/hdc_web`

```text
src
├─ api       HTTP与WebSocket通信
├─ assets    图片、字体等资源
├─ router    页面地址和路由
├─ store     本地配置和令牌
├─ utils     工具函数
├─ views     具体页面
├─ App.vue   最外层组件
└─ main.js   应用入口
```

推荐阅读顺序：

```text
main.js
→ App.vue
→ router/index.js
→ views中的页面
→ api中的请求
```

找按钮逻辑的方法：

- 在`views`中搜索按钮显示文字。
- 找到`@click`绑定的方法。
- 查看方法调用了哪个`api`。
- 根据接口路径找到后端Controller。

## 数字大屏目录

路径：`数字大屏前端code/hdc_digitallargescreen_web`

- `views/DataOverview.vue`：当前大屏总览页面。
- `api/Service.js`：HTTP请求封装。
- `api/WebSocket.js`：大屏实时连接。
- `router/index.js`：大屏和设置页面路由。

## Spring Boot后端目录

路径：`数字大屏后端code/hdc_server`

```text
controller   接收HTTP请求
service      处理业务逻辑
mapper       访问MySQL
entity       对应数据库表
dto          接口输入和输出
config       MQTT、WebSocket等配置
scheduled    定时任务
util         工具类
HdcApplication.java  后端入口
```

一条普通请求路径：

```text
前端页面
→ api/Service.js
→ Controller
→ Service
→ Mapper
→ MySQL
→ 返回JSON
```

## 当前运行服务与端口

- 管理端：`8080`。
- 数字大屏：`8081`。
- Spring Boot：`8088`。
- MySQL：`3307`，数据库`hdc`。
- MQTT Broker：`31883`。
- 后端上下文路径：`/hdc/api`。

正确启动顺序：

```text
MySQL
→ MQTT Broker
→ Spring Boot
→ 管理端与数字大屏
```

## 开发运行与演示部署

### Vue开发

```powershell
npm run serve
```

- 修改Vue文件后自动热更新。
- 两个前端目录已经存在`node_modules`，不要无原因反复执行`npm install`。

### Vue生产构建

```powershell
npm run build
```

- 生成静态文件交给Nginx。
- 开发期间不要每次修改都重新部署Nginx。

### Spring Boot开发

- 在IDE中运行`HdcApplication.java`。
- 看到`Started HdcApplication`才算启动成功。

### Spring Boot生产运行

- 构建或运行`hdc-2024031501.jar`。
- 后端必须能连接MySQL和MQTT。

## 浏览器开发者工具

### Elements

- 查看实际页面元素和样式。
- 检查元素是否被隐藏或覆盖。

### Console

- 查看JavaScript错误和调试日志。
- 当前部分代码关闭了`console.log`，排错时注意日志开关。

### Network

- 查看HTTP请求URL、方法、状态码、请求体和响应。
- `404`通常是路径错误。
- `500`通常需要查看后端日志。
- 请求长时间`pending`可能是后端未响应或网络问题。

### Network中的WS

- 查看WebSocket握手和消息帧。
- 检查`ping/pong`和业务消息。

## 固定故障排查顺序

1. 页面打不开：检查前端进程和端口。
2. 页面能打开但没数据：检查`Network`是否发出请求。
3. 请求`404`：检查前端路径、后端上下文和Controller映射。
4. 请求`500`：查看Spring Boot异常日志。
5. 数据库连接失败：检查MySQL进程、3307端口、用户名和密码。
6. 页面不实时更新：检查WebSocket连接和事件处理。
7. 后端收不到设备数据：检查MQTT Broker、主题、端口和订阅日志。
8. 设备收到命令但没动作：检查设备控制器和ACK，不要只改前端。

## Git协作

### 推荐分支

```text
main              随时可演示的稳定版本
feat/backend      后端和设备接入
feat/web          管理端和数字大屏
feat/harmony      鸿蒙工位应用
feat/ai           Python、视觉、RAG和MCP
```

### 不提交的内容

```text
node_modules
target
dist
运行日志
数据库运行文件
下载的软件包
模型缓存
本地密码和密钥
```

### 提交原则

- 一次提交只做一个明确功能。
- 提交前至少完成编译或最小运行验证。
- 不直接在`main`上多人同时修改。
- 合并前解决冲突并重跑完整流程。

提交示例：

```text
feat: 增加气体传感器数据接口
fix: 修复大屏WebSocket断线重连
```

## C盘空间规划

统一把大文件和缓存放E盘：

```text
E:\dev\npm-cache
E:\dev\maven-repository
E:\dev\DevEco-Studio
E:\dev\HarmonyOS-SDK
E:\dev\python-envs
E:\hdc_runtime
```

- 三台开发电脑统一工具版本。
- 不要让Node、Maven、Python、DevEco和模型缓存继续挤占C盘。

## 实操作业

- 指出管理端、大屏端和后端入口。
- 独立启动五个服务。
- 找到一次HTTP请求及其JSON响应。
- 从前端请求定位到Controller和Service。
- 修改一处页面文字并看到结果。
- 停止后端，观察前端错误并通过Network判断原因。
- 初始化Git仓库，配置忽略文件并完成第一次提交。

## 验收标准

随便关闭一个服务，学生能够在五分钟内判断关闭的是哪个服务，而不是逐个重启碰运气。

## 具体实操：第一次启动和排错

### 步骤1：打开PowerShell并进入项目

```powershell
Set-Location 'E:\2026挑战杯\工业沙盘2026\数字展板前后端'
Get-ChildItem
```

应该看到：

```text
数字大屏前端code
数字大屏后端code
.runtime
```

### 步骤2：检查工具是否可用

```powershell
java -version
node --version
npm --version
mvn --version
```

记录每个人输出的版本。版本明显不一致时先统一，不要先改代码。

### 步骤3：启动管理端

打开一个PowerShell窗口：

```powershell
Set-Location 'E:\2026挑战杯\工业沙盘2026\数字展板前后端\数字大屏前端code\hdc_web'
npm run serve
```

浏览器访问：

```text
http://localhost:8080
```

停止服务使用`Ctrl+C`。

### 步骤4：启动大屏

打开第二个PowerShell窗口：

```powershell
Set-Location 'E:\2026挑战杯\工业沙盘2026\数字展板前后端\数字大屏前端code\hdc_digitallargescreen_web'
npm run serve -- --port 8081
```

浏览器访问：

```text
http://localhost:8081/dataOverview
```

如果8081被占用，Vue会提示可用端口，记录实际端口。

### 步骤5：启动后端

在IDE中打开：

```text
数字大屏后端code\hdc_server\src\main\java\com\archermind\hdc\HdcApplication.java
```

运行`main`方法。控制台应出现：

```text
Started HdcApplication
```

如果失败，先检查MySQL和MQTT是否启动，再查看第一个`Caused by`。

### 步骤6：验证端口

```powershell
Test-NetConnection localhost -Port 8080
Test-NetConnection localhost -Port 8081
Test-NetConnection localhost -Port 8088
Test-NetConnection localhost -Port 3307
Test-NetConnection localhost -Port 31883
```

`TcpTestSucceeded : True`表示该端口有服务监听。

### 步骤7：完成一次前端到后端追踪

1. 打开管理端。
2. 按`F12`进入Network。
3. 点击一个页面按钮。
4. 找到状态码为200的请求。
5. 复制Request URL。
6. 在项目中搜索URL最后一段。
7. 找到对应Controller。
8. 再找到它调用的Service和Mapper。

### 步骤8：故意制造三种故障

#### 故障A：停止后端

- 关闭后端运行窗口。
- 刷新管理端。
- 在Network查看请求失败。
- 记录浏览器显示、HTTP错误和后端状态。

#### 故障B：请求错误路径

- 在浏览器Network中观察一个请求。
- 临时在开发者工具中记录其路径。
- 不直接改源码，先判断404来自路径还是后端未启动。

#### 故障C：WebSocket断开

- 打开Network的WS标签。
- 停止后端。
- 观察连接变为Closed。
- 重启后端，观察是否重连。

### 步骤9：建立版本管理

根目录执行：

```powershell
Set-Location 'E:\2026挑战杯\工业沙盘2026\数字展板前后端'
git init
git add .
git commit -m 'chore: import existing project'
```

如果不准备马上提交`node_modules`和`target`，先创建`.gitignore`再执行`git add`。提交前检查：

```powershell
git status --short
```

### 本课完成标志

- 四个人都能独立启动管理端、大屏和后端。
- 能用`Test-NetConnection`判断端口。
- 能从一个前端请求定位到Controller和Service。
- 能区分页面打不开、接口失败、数据库失败和WebSocket断开。

## 保姆式执行清单：把现有项目稳定跑起来

## 零基础软件准备

### 需要的软件

本项目实际需要的工具分为“必须”和“后续再装”两组，不要一次性把所有工具安装到系统盘。

必须工具：

~~~text
JDK 8：运行和打包现有 Spring Boot 后端
Maven 3.9.x：编译后端，放在 E:\dev\apache-maven-3.9.9
Node.js 18 或 20：运行两个 Vue2 前端
Git：保存可恢复版本
Chrome 或 Edge：页面和接口调试
Docker Desktop：后续运行 MySQL，Docker 数据目录必须改到 E 盘
~~~

后续工具：

~~~text
DevEco Studio：第 11 课开始安装，SDK 放 E 盘
Python 3.11：第 14、15 课使用，虚拟环境放 E 盘
MQTTX：可选图形化 MQTT 客户端，也可以先使用 Node 模拟器
Nginx：最后部署静态文件，开发阶段不需要安装
~~~

本机已经验证的结果是：Node v24.12.0、npm 11.6.2、Java 21.0.9，Maven 原来不存在。后端源码声明 Java 8，正式运行优先使用 JDK 8；当前 Java 21 已经完成一次离线编译验证，但不能据此保证所有运行时行为与 JDK 8 完全一致。

### E 盘安装和缓存规则

先创建目录：

~~~powershell
New-Item -ItemType Directory -Force -Path 'E:\dev','E:\dev\maven-repository','E:\dev\npm-cache','E:\dev\python-envs','E:\hdc_runtime'
~~~

Maven 使用 E 盘仓库：

~~~powershell
$env:MAVEN_USER_HOME = 'E:\dev\maven-home'
& 'E:\dev\apache-maven-3.9.9\bin\mvn.cmd' '-Dmaven.repo.local=E:/dev/maven-repository' '-version'
~~~

Node 使用 E 盘 npm 缓存：

~~~powershell
npm config set cache 'E:\dev\npm-cache' --global
npm config get cache
~~~

如果 npm 缓存仍返回 C:\Users\...，先修正缓存路径，不要继续安装依赖。

### 当前项目的真实构建门禁

~~~powershell
$mvn='E:\dev\apache-maven-3.9.9\bin\mvn.cmd'
& $mvn '-o' '-Dmaven.repo.local=E:/dev/maven-repository' '-DskipTests' 'compile'
Set-Location 'E:\2026挑战杯\工业沙盘2026\数字展板前后端\数字大屏前端code\hdc_web'
npm run build
Set-Location '..\hdc_digitallargescreen_web'
npm run build
~~~

三条命令都返回成功，才允许开始修改业务代码。当前已验证后端和两个前端均可构建；前端资源体积警告暂不阻断开发。

### 软件与用途

- JDK 8：编译和运行Spring Boot。
- Maven：下载Java依赖、编译和打包后端。
- Node.js和npm：运行Vue前端以及Node模拟器。
- Git：保存版本、查看差异和回退自己的修改。
- IntelliJ IDEA或Eclipse：Java断点调试。
- VS Code：编辑Vue、JSON、Python和Markdown。
- MySQL客户端：查看数据库和执行SQL。
- MQTTX或MQTT Explorer：查看主题、发布测试消息和订阅ACK。
- Chrome或Edge：调试前端HTTP、WebSocket和页面状态。
- DevEco Studio：第11课安装鸿蒙开发环境。
- Python：第14、15课安装视觉和AI服务环境。

### E盘安装原则

运行环境统一规划：

```text
E:\dev\jdk8
E:\dev\apache-maven
E:\dev\nodejs
E:\dev\maven-repository
E:\dev\npm-cache
E:\dev\Git
E:\dev\DevEco-Studio
E:\dev\HarmonyOS-SDK
E:\hdc_runtime
```

安装器出现安装目录、SDK目录、缓存目录、仓库目录或Gradle目录时，都改到E盘。安装完成后关闭安装器，用命令验证，不以“安装成功”窗口为准。

### 安装后验证

在PowerShell逐行执行：

```powershell
java -version
javac -version
mvn -version
node --version
npm --version
git --version
python --version
```

把每个版本记录到`培训作业/环境版本.md`。四台电脑的版本明显不一致时，先统一开发环境。

### JDK和Maven调试

后端要求Java 8兼容。检查实际调用的是哪个Java：

```powershell
where.exe java
where.exe javac
```

如果`java -version`不是8，检查IDE项目JDK和系统`JAVA_HOME`。Maven仓库设置到：

```text
E:\dev\maven-repository
```

后端首次编译：

```powershell
Set-Location 'E:\2026挑战杯\工业沙盘2026\数字大屏后端code\hdc_server'
mvn -DskipTests compile
```

实际目录不确定时先查找：

```powershell
Get-ChildItem 'E:\2026挑战杯\工业沙盘2026' -Recurse -Filter pom.xml
```

### Node和npm调试

```powershell
npm config set cache E:\dev\npm-cache
npm config get cache
```

前端已经有`node_modules`时先执行`npm run serve`，只有出现模块缺失才安装依赖。不要在两个前端目录混用终端。

常见错误：

- `vue-cli-service not recognized`：当前目录错误或依赖缺失。
- OpenSSL错误：先使用项目自带的`NODE_OPTIONS=--openssl-legacy-provider`脚本。
- 端口占用：执行`Get-NetTCPConnection -LocalPort 8080`，只停止自己启动的进程。

### Git最小使用方法

```powershell
git status
git diff
git add 具体文件
git diff --cached
git commit -m 'feat: describe one small change'
```

提交前必须看差异。发现别人的修改不要覆盖。撤销自己最后一次已提交的功能使用`git revert 提交编号`，不要使用破坏团队文件的强制重置。

### IDE和浏览器怎么调试

Java：在Controller和Service左侧打断点，使用Debug运行，观察请求参数、调用栈和异常行。

Vue：打开`F12`：

- Console：JavaScript错误。
- Network：HTTP URL、请求体、响应和状态码。
- Network→WS：WebSocket连接与消息帧。
- Application：LocalStorage和令牌。
- Sources：浏览器实际加载的文件。

### 第二课环境门禁

```text
[ ] Java、Maven、Node、npm、Git可执行
[ ] Maven和npm缓存位于E盘
[ ] IDE能运行后端
[ ] 两个前端能运行
[ ] 浏览器能查看HTTP和WS
[ ] MySQL、MQTT端口可检查
[ ] 版本已记录并提交
```

### 第1步：不要先改代码，先复制项目

在文件管理器中复制整个项目根目录，形成一个开发副本：

```text
E:\2026挑战杯\工业沙盘2026\数字展板前后端
E:\2026挑战杯\工业沙盘2026\数字展板前后端-dev
```

后续培训和新功能只在`-dev`副本操作，原目录作为可回退基线。不要用`git reset --hard`代替备份。

### 第2步：确认源代码目录

逐个执行：

```powershell
Test-Path 'E:\2026挑战杯\工业沙盘2026\数字展板前后端\数字大屏前端code\hdc_web\package.json'
Test-Path 'E:\2026挑战杯\工业沙盘2026\数字展板前后端\数字大屏前端code\hdc_digitallargescreen_web\package.json'
Test-Path 'E:\2026挑战杯\工业沙盘2026\数字展板前后端\数字大屏后端code\hdc_server\pom.xml'
```

三个结果都必须是`True`。如果不是，先修正工作目录，不要在错误目录执行npm或Maven。

### 第3步：记录版本，不要凭感觉升级

执行并把结果保存到培训作业：

```powershell
java -version
node --version
npm --version
mvn --version
```

后端`pom.xml`要求Java 8兼容。前端是Vue 2和Vue CLI 3，安装依赖时优先使用已有`node_modules`，避免升级锁文件。

### 第4步：按服务顺序启动

每个服务单独开一个PowerShell窗口，并在窗口标题中写服务名。

#### MySQL

使用已经部署在`E:\hdc_runtime`的实例，先确认3307：

```powershell
Test-NetConnection 127.0.0.1 -Port 3307
```

#### MQTT

```powershell
Set-Location 'E:\2026挑战杯\工业沙盘2026\数字展板前后端\.runtime\mqtt-broker'
node .\start-broker.js
```

看到`MQTT broker listening on 127.0.0.1:31883`才继续。

#### Spring Boot

在IDE打开`HdcApplication.java`运行。启动失败时只看第一个`Caused by`，按顺序处理数据库、MQTT、端口和Java版本。

#### 管理端

```powershell
Set-Location 'E:\2026挑战杯\工业沙盘2026\数字展板前后端\数字大屏前端code\hdc_web'
npm run serve
```

#### 大屏

```powershell
Set-Location 'E:\2026挑战杯\工业沙盘2026\数字展板前后端\数字大屏前端code\hdc_digitallargescreen_web'
npm run serve -- --port 8081
```

### 第5步：逐项健康检查

不要一次打开所有页面后凭感觉判断“能不能用”。按顺序检查：

```text
1. 3307可连接
2. 31883可连接
3. 后端日志Started
4. 管理端8080能打开
5. 大屏8081能打开
6. 管理端Network有HTTP请求
7. 大屏Network有dataScreen WebSocket
8. 后端日志有MQTT连接和订阅
```

每一步失败都停下，记录失败位置和日志，不跳到下一步。

### 第6步：建立Git基线

在开发副本根目录执行：

```powershell
Set-Location 'E:\2026挑战杯\工业沙盘2026\数字展板前后端-dev'
git init
```

先创建`.gitignore`，至少忽略：

```text
node_modules/
target/
dist/
.runtime/
*.log
```

然后：

```powershell
git add .
git status --short
git commit -m 'chore: establish runnable baseline'
```

每次开始改代码前先确认：

```powershell
git status --short
```

发现不是自己改的文件时不要覆盖，先保存并确认来源。

### 第7步：第一次问题定位演练

按顺序故意停止服务：

1. 停止后端，刷新管理端，记录Network错误。
2. 启动后端但停止MySQL，记录后端启动日志。
3. 启动MySQL但停止MQTT，记录MQTT连接日志。
4. 停止大屏，确认管理端不受影响。
5. 恢复所有服务，再检查页面是否回到最新状态。

### 第8步：完成第二课门禁

```text
[ ] 开发副本和原始基线分离
[ ] Java、Node、npm、Maven版本记录
[ ] MySQL、MQTT、后端、两个前端能按顺序启动
[ ] 五个端口能检查
[ ] Git基线提交完成
[ ] 四个人能独立定位三种故障
```
