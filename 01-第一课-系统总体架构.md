# 第一课：系统总体架构与完整数据流

## 本节学习路线

### 这是什么

这是整条产线的软件地图，说明设备、通信、后端、数据库、AI、视觉和三个界面分别做什么。

### 为什么要学

如果不先划清边界，大家会出现重复开发、前端直接控制设备、AI绕过后端、真实设备没有模拟替代等问题，最后无法联调。

### 它在项目中的位置

本节是全部课程的前置条件。第2课的环境、第3课的接口、第4课的数据库，以及后续后端、鸿蒙和AI，都必须以本节的编号、状态和通信边界为准。

### 需要的软件

- PowerShell：执行检查命令。
- VS Code：编辑设备表、接口草图和Markdown。
- draw.io或ProcessOn：画架构图和流程图。
- 浏览器：查看已经运行的管理端、大屏和后端接口。

本节不需要安装数据库、模型或鸿蒙工具，先完成设计和编号。

### 最小例子

只追踪一只虚拟瓶BOT-DEMO-001：从CAM-01识别为PLA-500，经过水洗、气体检测、外观检测、灌装、二检、装箱、AGV和仓储。每一步写清楚数据来源、保存位置、发送方式和显示界面。

### 项目实操顺序

1. 建立设备编号表。
2. 固定正常瓶、缺陷瓶、批次、箱子、AGV任务和库位编号。
3. 画设备层、平台层和界面层。
4. 给每条箭头标注HTTP、MQTT或WebSocket。
5. 写正常流程、气体超限流程和设备离线流程。
6. 四个人轮流口述完整链路。

### 如何调试设计错误

- 一条箭头没有发布方或接收方：通信设计不完整。
- 一个状态在不同模块叫不同名字：枚举没有冻结。
- 页面自己判断灌装放行：业务边界错误。
- AI输出直接发MQTT：安全边界错误。
- 真实设备没有模拟器：联调顺序错误。

### 如何与下一节连接

本节完成的设备编号、服务端口、通信方式和演示数据，会被第2课启动环境、第3课接口表和第4课数据库直接使用。

### 通过门禁

只有设备表、两条流程、架构图和边界规则全部完成，才能进入第2课。未完成时不开始大范围写页面。

## 学习目标

- 认识设备层、平台层和界面层。
- 讲清一只瓶子从识别到入库时，数据怎样流动。
- 明确AI、Spring Boot、数据库、MQTT和三类界面的边界。
- 理解谁可以控制设备，谁只能展示或提出建议。

## 系统组成

### 设备层

- 摄像头：拍摄瓶型、瓶盖、瓶身和瓶底图像。
- 气体传感器：检测PLA瓶处理后的有害气体种类和浓度。
- 温湿度传感器：监测饮料生产和仓储环境。
- 传送带：负责瓶子在工位之间移动。
- 灌装和封盖设备：完成饮料灌装与简化封盖。
- 机械臂：按照装箱算法给出的坐标夹取和摆放瓶子。
- AGV：负责箱体运输，超声波避障在小车本地完成。
- PLC、单片机或设备控制器：读取传感器并执行电机、泵、机械臂等动作。

### MQTT消息服务

- MQTT Broker是设备消息中转站。
- 设备通过主题发布数据，Spring Boot订阅主题接收数据。
- Spring Boot通过命令主题向设备下发动作，设备通过ACK主题返回结果。
- MQTT只传递消息，不保存完整生产业务，也不判断是否放行。

示例主题：

```text
设备上报：factory/line1/gas/GAS-01/telemetry
控制命令：factory/line1/wash/PUMP-01/command
执行反馈：factory/line1/wash/PUMP-01/ack
```

### Spring Boot业务后端

- 接收设备数据并保存。
- 维护瓶子、饮料批次、箱子、AGV任务和库存状态。
- 执行状态机和工序放行规则。
- 调用视觉服务和AI服务。
- 校验AI建议是否安全、是否越界。
- 生成设备命令并等待真实执行反馈。
- 向管理端、大屏和鸿蒙工位屏提供统一数据。

关键原则：Spring Boot是设备状态和生产状态的唯一可信来源。

### MySQL数据库

- 保存瓶型、尺寸、工艺参数和版本。
- 保存设备、工位和安全上下限。
- 保存生产批次、单瓶追踪、检测、装箱、运输和入库记录。
- 保存报警、操作、命令和AI决策记录。
- 页面、鸿蒙端和AI不能直接连接数据库，必须通过后端访问。

### Python AI服务

- 视觉识别：识别瓶型与瓶盖、瓶身、瓶底缺陷。
- RAG：从本地工艺文档中检索相关知识和依据。
- MCP：让AI调用查询瓶型、工艺参数、设备状态和安全阈值等受控工具。
- AI输出结构化建议，不直接下发设备命令。

示例建议：

```json
{
  "bottleType": "PLA-500",
  "recommendedSpeed": 0.25,
  "recommendedFillVolume": 500,
  "reason": "匹配PLA-500当前有效工艺",
  "risk": "NONE"
}
```

### Web管理端

- 设备、工位、瓶型和工艺参数管理。
- 生产批次、报警、物流、仓储和权限管理。
- 生产追溯、AI决策与操作记录查询。
- 管理员可以提交操作请求，但仍需后端校验。

### Web数字大屏

- 展示整条产线状态、产量、良品率和报警。
- 展示装箱、AGV、仓储和环境安全状态。
- 面向参观和监控，原则上不承担复杂控制。

### 鸿蒙工位小屏

- 一个ArkTS应用支持多个工位，根据工位编号显示不同页面。
- 查看本站设备、传感器、瓶子和任务状态。
- 调整被允许的参数，提交启停和报警确认请求。
- 不能绕过Spring Boot直接控制设备。

## 系统中流动的六类数据

- 遥测数据：气体浓度、温度、湿度、AGV速度和距离。
- 设备状态：空闲、运行、暂停、故障、离线。
- 检测结果：瓶型、缺陷、合格、不合格、置信度。
- 工艺参数：传送带速度、灌装量、处理时间、机械臂坐标。
- 控制命令：启动、停止、调整参数、剔除、运输。
- 业务记录：批次、装箱、运输、入库、报警和决策。

重要消息至少包含：

```json
{
  "messageId": "MSG-20260718-0001",
  "deviceId": "GAS-01",
  "timestamp": "2026-07-18T10:20:30+08:00",
  "value": 6.2,
  "unit": "ppm"
}
```

## 完整生产流程

### 瓶子支线

- 摄像头识别瓶型。
- 后端创建单瓶追踪编号。
- AI查询RAG和数据库，提出工艺参数建议。
- 后端校验建议并加载有效工艺版本。
- 模拟PLA瓶生产后的静置、冷却、水洗和风洗。
- 气体传感器检测有害气体浓度。
- 气体合格后进入第一次外观检测。
- 检查瓶盖、瓶身、瓶底等可见缺陷。

### 饮料支线

- 模拟原料混合和调配。
- 温湿度传感器监测生产环境。
- 模拟高温杀菌处理。
- 饮料批次通过质量检查后进入待灌装状态。

### 汇合条件

```text
允许灌装 = 瓶子支线合格 AND 饮料批次合格
```

- 任一支线未完成或不合格，后端都不能允许灌装。
- 汇合后执行灌装、简化封盖和二次外观检测。
- 二检合格后计算装箱位置并由机械臂摆放。
- 创建AGV运输任务，AGV本地执行超声波避障。
- 到达仓储区后完成入库、库位和库存更新。
- 仓储气体、烟雾、火灾和温湿度持续监测。

## 工位屏点击按钮后的完整过程

以“启动水洗”为例：

1. 鸿蒙应用向Spring Boot提交启动请求。
2. 后端检查登录人员权限。
3. 检查瓶子是否到达水洗工位及前序工序是否完成。
4. 检查设备是否在线、是否故障、是否存在活动报警。
5. 检查参数是否位于安全范围。
6. 校验通过后生成唯一`commandId`并保存`PENDING`记录。
7. 后端通过MQTT向设备控制器发布命令。
8. 控制器执行水洗并返回ACK。
9. 后端把命令更新为`SUCCESS`、`FAILED`或`TIMEOUT`。
10. 后端更新工序状态并通过WebSocket推送到三个界面。

按钮显示“已发送”不代表设备已运行；只有收到设备ACK，才能显示最终执行结果。

## 异常处理示例

### 气体浓度超限

```text
传感器上报超限
→ 后端校验数值和单位
→ 创建报警记录
→ 禁止进入下一工序
→ 必要时发送停线命令
→ 工位屏弹出报警
→ 大屏显示红色状态
→ 管理端记录确认人和处理过程
```

### AI服务不可用

- 基础安全规则继续运行。
- 使用已经审核的工艺版本或转人工确认。
- 禁止新的AI自动参数调整。
- AI是增强能力，不能成为安全控制的唯一依赖。

## 必须遵守的架构规则

- 前端和鸿蒙端不能直接操作数据库。
- 工位屏不能绕过后端直接控制设备。
- AI不能直接下发设备命令。
- AGV实时避障必须在本地控制器完成。
- 所有控制命令必须有编号、执行反馈和超时。
- 所有传感器数据必须有设备、时间和单位。
- 模拟设备与真实设备使用同一消息格式。
- 三类界面必须显示同一后端的状态。

## 实操作业

- 画出系统架构图并标注已有和待开发模块。
- 制作设备表：设备编号、工位、数据、单位、动作、通信方式。
- 写出正常生产、气体超限和设备离线三条流程。
- 每个人口述一只瓶子从识别到入库的数据流。

## 验收问题

### 问题

鸿蒙工位屏点击“启动灌装”后，系统如何判断能不能执行，如何确认设备真的执行，三个界面怎样同时获得最新状态？

### 正确要点

- Spring Boot检查权限、流程、设备、参数和报警。
- 生成命令编号并通过MQTT发送。
- 必须收到设备ACK才能确认执行结果。
- 后端保存状态后通过WebSocket推送到管理端、大屏和鸿蒙屏。

## 具体实操：画出你们自己的系统图

### 步骤1：创建工作目录

在项目根目录下新建一个只用于培训资料的目录。这个目录可以手动在文件管理器中创建，不需要安装任何软件：

```text
E:\2026挑战杯\工业沙盘2026\数字展板前后端\培训作业
```

在其中准备三个文件：

```text
01-系统架构.md
02-设备清单.md
03-三条异常流程.md
```

### 步骤2：填写设备清单

每一行只写一个设备，按照下面字段填写：

```text
设备编号 | 设备名称 | 所属工位 | 输入数据 | 单位 | 可执行动作 | 通信方式 | 真实/模拟
```

先填写已经拥有的设备：

```text
GAS-01 | 气体传感器 | GAS-01 | VOC浓度 | ppm | 无 | MQTT | 真实
CAM-01 | 检测摄像头 | INSPECT-01 | 图片 | jpg | 拍照 | HTTP | 真实
AGV-01 | AGV小车 | AGV-01 | 速度、距离、负载 | m/s、m、kg | 运输、停止 | MQTT | 真实
ARM-01 | 机械臂 | PACK-01 | 运行状态 | - | 抓取、放置 | MQTT/控制器 | 真实
```

### 步骤3：用一只虚拟瓶子走流程

固定一只测试瓶，编号为`BOT-TEST-001`，依次口述并记录：

1. 摄像头得到瓶型`PLA-500`。
2. 后端创建批次和单瓶追踪记录。
3. AI返回工艺建议，后端校验。
4. 水洗工位执行完成。
5. 气体传感器返回`6.2 ppm`，判定合格。
6. 外观检测返回`PASS`。
7. 饮料批次返回`READY_FOR_FILLING`。
8. 后端允许灌装和封盖。
9. 二检返回`PASS`。
10. 机械臂把瓶子放入箱子。
11. AGV接收运输任务。
12. 仓储系统完成入库。

每一步都回答三个问题：

```text
谁产生数据？
后端保存什么？
哪个界面需要看到变化？
```

### 步骤4：画三层箭头

在纸上或绘图软件中画三层：

```text
第一层：设备和传感器
第二层：MQTT、Spring Boot、MySQL、AI、视觉
第三层：管理端、大屏、鸿蒙工位屏
```

每条箭头写上通信方式，例如：

```text
GAS-01 --MQTT telemetry--> Spring Boot
Spring Boot --WebSocket alarm.created--> 大屏
鸿蒙屏 --HTTP station-commands--> Spring Boot
Spring Boot --MQTT command--> PUMP-01
```

### 步骤5：模拟一次气体超限

使用测试值`12.5 ppm`，填写事件链：

```text
传感器上报12.5 ppm
→ 后端查询阈值10 ppm
→ 创建报警
→ 瓶子进入HOLD
→ 禁止灌装
→ 三个界面收到alarm.created
```

### 步骤6：四个人轮流讲解

每个人随机抽一个节点，从数据产生处讲到页面显示处。讲不清楚的地方直接在架构图上补箭头，直到四个人都能复述完整链路。

### 本课完成标志

- 设备表中每个已有设备都有编号和通信方式。
- 一只测试瓶的12个步骤都能说清楚。
- 正常和气体超限两条流程都画出来。
- 四个人能指出每一步的数据来源、保存位置和显示界面。

## 保姆式执行清单：本项目从零开始的第一天

本节不写业务代码，先把项目边界锁死。任何人没有完成下面清单，都不能开始随意新增页面或设备接口。

### 第1步：建立统一工作区

所有代码和运行文件只放在E盘，约定如下：

```text
项目源码：E:\2026挑战杯\工业沙盘2026\数字展板前后端
运行环境：E:\hdc_runtime
新增AI：E:\bottling-ai
新增视觉：E:\bottling-vision
新增模拟器：E:\bottling-device-simulator
鸿蒙工程：E:\bottling-harmony
演示数据备份：E:\hdc_runtime\backup
```

不要把模型、数据库、日志、Node缓存、Python环境或鸿蒙SDK放到C盘。

### 第2步：给所有设备编唯一编号

先不要使用中文名称作为程序判断条件。按下面规则建立编号：

```text
GAS-01       气体传感器
TEMP-01      温湿度传感器
CAM-01       瓶型和初检摄像头
CAM-02       二检摄像头
CONV-01      传送带
WASH-01      水洗工位
AIR-01       风洗工位
FILL-01      灌装工位
CAP-01       封盖工位
ARM-01       机械臂
AGV-01       AGV
WH-GAS-01    仓储气体传感器
```

编号写入设备表后不再随意修改。页面显示中文名称，接口和MQTT使用编号。

### 第3步：只保留一条演示主线

第一版固定一只正常瓶完整走通：

```text
BOT-DEMO-001
→ PLA-500
→ BATCH-DEMO-001
→ DRINK-DEMO-001
→ BOX-DEMO-001
→ AGV-TASK-DEMO-001
→ WH-LOC-A01
```

另准备一只缺陷瓶用于异常演示：

```text
BOT-DEMO-002
→ PLA-500
→ 二次检测 CAP_TILTED
→ REJECTED
```

主流程和异常流程不要混在同一只瓶子里，否则演示状态难以恢复。

### 第4步：确定唯一状态来源

四个人在纸上写出下面结论并签字确认：

```text
设备当前状态：Spring Boot内存最新值 + MySQL关键状态
瓶子当前状态：MySQL bottle表
历史过程：MySQL process_event表
设备原始消息：MQTT进入Spring Boot后处理
界面实时状态：Spring Boot通过WebSocket推送
AI建议：ai_decision表保存，不能直接当执行结果
```

任何页面出现与数据库不同的状态，先查后端和事件顺序，不允许在前端偷偷修正。

### 第5步：画一条真正能执行的闭环

必须画出以下箭头，而且每条箭头写通信方式：

```text
CAM-01 --HTTP/视觉结果--> Spring Boot
Spring Boot --HTTP--> AI服务
AI服务 --RAG/MCP--> 知识库和受控后端工具
Spring Boot --MQTT command--> WASH-01
WASH-01 --MQTT ack--> Spring Boot
GAS-01 --MQTT telemetry--> Spring Boot
Spring Boot --HTTP/WebSocket--> 管理端
Spring Boot --HTTP/WebSocket--> 数字大屏
Spring Boot --HTTP/WebSocket--> 鸿蒙工位屏
```

如果某条箭头没有发布方、接收方、字段和失败处理，这条链路还没有设计完成。

### 第6步：完成第一课门禁

负责人逐项打勾：

```text
[ ] 设备编号表完成
[ ] 正常瓶和缺陷瓶编号固定
[ ] 每个模块的通信方式固定
[ ] 瓶子和饮料两条支线画出
[ ] 灌装汇合条件写出
[ ] 气体超限流程写出
[ ] AI不能直控设备的规则确认
[ ] 四个人能口述完整链路
```

任何一项未完成，下一课只允许继续补设计，不开始大范围编码。
