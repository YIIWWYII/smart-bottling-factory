# 智慧装瓶工厂软硬件对接需求

**文档版本：** v0.1（企业对接草案）
**文档状态：** 待双方确认
**适用对象：** 企业硬件/自动化/网络/软件对接人员、项目组后端/AI/鸿蒙前端人员

> 本文是对接边界和交付要求，不是设备采购合同，也不是最终 PLC、MQTT 或视觉协议。文中标记为“待确认”的内容必须由企业提供现场资料或由双方联调后冻结。当前仓库中的 `SIMULATION`、`LOCAL DEMO` 和示例阈值不能直接作为现场生产参数。

## 1. 对接目标

企业负责将装瓶生产线及其控制、视觉、物流、仓储系统接入本项目；项目组负责把真实数据接入现有 Spring Boot 后端、AI 中枢和三个鸿蒙应用，形成可观察、可追踪、可受控、可验收的闭环。

目标闭环如下：

```text
设备/传感器/相机/AGV
        |
        v
PLC、边缘控制器或企业网关
        |
        | MQTT / HTTPS / 企业既有接口
        v
Spring Boot 生产后端 <------> MySQL
        |  \
        |   \ HTTP / WebSocket
        |    \
        |     +--> 数字展板、工位终端、后台管理
        |
        +------> AI 中枢：视觉、RAG、实时事实、决策建议
                         |
                         +--> 只通过后端安全闸口提交命令意图
```

必须满足：

1. 前端不直接连接传感器、PLC、MQTT Broker 或设备 SDK。
2. 企业硬件/边缘控制层负责物理安全、急停、联锁和设备保护；软件系统不能替代物理安全回路。
3. 后端接收数据后保存来源、时间、质量和版本，不能把模拟数据伪装成真实数据。
4. AI 可以识别瓶型、查询知识和提出字段级建议，但不能绕过后端直接控制设备。
5. “后端受理命令”与“设备实际执行成功”必须通过边缘层回执区分。

## 2. 当前项目基线

| 模块 | 当前职责 | 默认地址/位置 | 真实接入时的关系 |
| --- | --- | --- | --- |
| 数字展板 | 产线总览、工序详情、只读 2D/3D Three.js | `apps/digital-display` | 读取后端快照和实时事件 |
| 工位终端 | 本站设备、参数、受控操作和回执 | `apps/workstation-terminal` | 读取本站数据，写操作经后端安全校验 |
| 后台管理 | 认证、生产、设备、质量、物流仓储、AI 配置、知识和审计 | `apps/admin-console` | 管理配置和审核，不直接接硬件 |
| 生产后端 | Spring Boot 业务状态、持久化、实时推送和安全闸口 | `services/backend/hdc_server`，默认 `8088` | 接收企业边缘数据，向外提供业务 API |
| AI 中枢 | Python 服务，助手、视觉、RAG、决策接口 | `services/ai-center`，默认 `8091` | 通过后端只读事实和命令意图接口工作 |
| MySQL | 账号、生产事实、设备、产品、命令、审计和配置 | 企业部署决定 | 必须由后端访问，前端和 AI 不直接持有生产库密码 |
| 共享助手 | 三端共用的 ArkUI HAR | `packages/harmony-assistant` | 统一问答、流式响应、引用和权限反馈 |

当前项目已具备软件模拟联调基础，但以下内容仍需企业现场资料和正式接入：真实设备点位、边缘网关、视觉模型、AGV/WMS 接口、现场阈值、命令回执、网络安全和长时间运行验收。

## 3. 产线范围

下表是当前软件中的九个工序。企业应按实际产线调整设备编码和工艺内容，不要求现场设备名称必须与示例完全相同，但最终编码必须稳定且一一对应。

| 工序代码 | 工序 | 当前软件关注的设备/系统 | 企业需要提供的真实对象 | 最低放行条件 |
| --- | --- | --- | --- | --- |
| `PRETREATMENT` | 预处理 | 输送机、水洗、风洗、工位控制器 | 冷却/静置、水洗、风洗设备及 PLC/边缘点位 | 清洗、风洗完成，联锁正常 |
| `GAS_INSPECTION` | 气体检测 | 密封检测舱、VOC 传感器、采样泵、报警灯 | 密封采样装置、气体传感器、采样和报警控制 | 气体种类与浓度满足企业安全阈值 |
| `APPEARANCE_INSPECTION` | 外观检测 | 顶部/侧面/底部相机、光源、剔除机构 | 相机、光源、触发、视觉模型、剔除执行器 | 瓶盖、瓶身、瓶底判定通过或已隔离 |
| `BEVERAGE_READY` | 饮料准备 | 混合调配、高温处理、温度/湿度传感器 | 配料/混合、杀菌设备、温湿度采集和控制点 | 配方、杀菌记录和温度范围满足要求 |
| `FILLING` | 灌装 | 定位、灌装泵、灌装头、流量计、液位检测 | 灌装执行器、流量/液位检测、瓶型换型参数 | 灌装量、液位和配方校验通过 |
| `SECONDARY_INSPECTION` | 二次检测 | 二检相机、液位相机、光源、剔除机构 | 瓶盖/封装/液位复核设备和剔除回执 | 复检通过或不合格品隔离 |
| `PACKING` | 机械臂装箱 | 六轴机械臂、柔性夹爪、箱体相机、箱码打印、计数器 | 机械臂、夹具、箱体识别、码制和装箱回执 | 排列、数量、箱码和产品追踪绑定正确 |
| `AGV_TRANSPORT` | AGV 运输 | AGV、超声波、里程计、负载、调度服务 | AGV 控制接口、避障、定位、负载和调度接口 | 任务完成、障碍距离和负载满足安全规则 |
| `WAREHOUSE_INBOUND` | 仓储入库 | VOC、烟雾、库位灯、WMS | 仓区安全传感器、库位系统、WMS/库存系统 | 仓区安全，箱码和库位登记成功 |

饮料准备与瓶体处理可以并行运行。系统不能用“第一批产品完成”作为整线完成条件，而应按产品追踪号、工序缓冲和各工位实时状态独立处理。

## 4. 双方责任边界

### 4.1 企业需要交付

企业在联调前至少提供以下资料和条件：

1. 完整设备清单、设备厂家、型号、固件/软件版本、设备唯一编码和所属工序。
2. PLC、边缘控制器或网关的品牌、型号、网络地址、协议能力和点位表。
3. MQTT Broker 或企业既有接口的地址、端口、TLS 要求、账号、证书、Topic 规则和 ACL。
4. 每个设备的遥测点、单位、量程、精度、采样周期、质量码和异常码。
5. 每个可控参数/动作的命令名称、参数范围、步长、风险等级、前置联锁和执行回执格式。
6. 视觉相机的触发方式、图片/视频输出方式、分辨率、帧率、曝光要求和推理接口；如果企业提供视觉模型，还要提供模型版本和缺陷标签字典。
7. AGV 的任务、路线、定位、避障、负载和状态接口；WMS 的箱码、库位、库存和入库接口。
8. 现场网络拓扑、IP 规划、VLAN/防火墙规则、时间同步方式和远程联调方式。
9. 可安全重复的测试样品、合格样品、不合格样品和故障模拟方法。
10. 安全操作规程、急停/联锁说明、校准证书、维护周期和现场联系人。

### 4.2 项目组负责

1. 将企业确认后的点位和协议映射到 Spring Boot 业务模型。
2. 实现或完善 MQTT/HTTP/企业接口适配，不让鸿蒙前端直接访问硬件。
3. 将遥测、事件、产品追踪、质量门、命令和回执保存并推送到三个前端。
4. 根据企业提供的瓶型、配方、设备能力和阈值资料接入后台管理和 AI 中枢。
5. 实现 AI 视觉结果、RAG 检索、数据库校验和字段级命令意图的业务闭环。
6. 提供模拟数据、接口测试、断线重连、重复消息、异常回执和权限测试。
7. 提供部署说明、联调记录、版本记录和验收证据。

### 4.3 双方共同完成

- 冻结设备编码、工序编码、单位、时间格式、状态枚举、错误码和版本策略。
- 确认真实安全阈值、质量放行规则、剔除/返工/暂停策略和恢复条件。
- 逐台设备完成数据接入测试，再进行单工序和整线联调。
- 对每个控制动作进行“请求、后端校验、边缘执行、回执、前端展示”全链路验收。

## 5. 协议和数据流要求

### 5.1 协议职责

| 数据流 | 推荐协议 | 方向 | 内容 | 是否允许前端直接调用 |
| --- | --- | --- | --- | --- |
| 设备遥测 | MQTT 或企业边缘协议 | 设备/边缘 -> 后端 | 设备状态、传感器、计数、速度、质量码 | 不允许 |
| 设备事件 | MQTT 或企业边缘协议 | 设备/边缘 -> 后端 | 工序完成、剔除、故障、产品流转 | 不允许 |
| 设备控制 | MQTT/OPC UA/企业网关协议 | 后端 -> 边缘 -> 设备 | 参数、启停、复位、调度和动作命令 | 不允许 |
| 设备回执 | MQTT/企业接口 | 边缘 -> 后端 | 接收、执行、失败、超时和设备回执号 | 不允许 |
| 业务查询/写操作 | HTTP/HTTPS | 鸿蒙端 <-> 后端 | 登录、快照、配置、命令请求、审计 | 只允许调用后端 |
| 实时展示 | WebSocket | 后端 -> 鸿蒙端 | 产线、工位、设备、产品、报警和命令事件 | 只允许订阅后端 |
| AI 问答 | HTTP/WebSocket | 鸿蒙端 <-> AI 中枢 | 会话、流式回答、引用和错误 | 通过共享助手 |
| AI 生产事实 | HTTP 或内部工具协议 | AI 中枢 <-> 后端 | 只读快照、识别结果、决策和命令意图 | 前端不调用 |

MQTT 是消息载体，不是业务数据本身。Topic、消息体、时间、版本、幂等号和回执语义必须一起确定。

### 5.2 当前可作为接入起点的 MQTT 约定

当前后端代码已监听以下模式，企业可以此为第一版联调起点，但正式上线前必须由双方冻结：

```text
factory/{lineId}/{stageCode}/{deviceCode}/telemetry
factory/{lineId}/{stageCode}/{deviceCode}/event
```

当前实现使用 QoS 1 接收消息，并将消息中的 `stageCode`、`deviceCode` 与 Topic 互相补充。建议企业在消息体中仍然完整携带这些字段，避免网关转发后丢失上下文。

当前演示代码会从 Topic 补充 `stageCode` 和 `deviceCode`；`lineId` 仍需在正式适配时纳入后端映射和校验，不能仅依赖 Topic 文本推断产线归属。

遥测消息建议格式：

```json
{
  "schemaVersion": 1,
  "eventId": "evt-20260822-000001",
  "lineId": "LINE-01",
  "stageCode": "GAS_INSPECTION",
  "deviceCode": "GAS-VERIFY-01",
  "occurredAt": "2026-08-22T14:30:00.125+08:00",
  "sequence": 18342,
  "state": "RUNNING",
  "source": "MQTT",
  "speedMps": 0.20,
  "progress": 0.65,
  "metrics": {
    "vocPpm": 4.6,
    "temperatureC": 25.1
  }
}
```

最低要求：

- `eventId` 全局或至少在设备范围内唯一，便于去重。
- `occurredAt` 是设备采集时间，不使用后端收到消息的时间替代；所有设备必须校时。
- `sequence` 单调递增，设备重启后要有明确重置规则。
- `state` 使用双方确认的枚举，未知状态不能默认为正常。
- 每个数值必须带单位或使用已冻结的字段名；不能把 `4.6` 这种无单位数字直接交付。
- `quality`、校准状态、数据来源和故障码必须可追踪。
- 断网期间边缘层需要明确缓存容量、补发策略和数据是否可能丢失。

工序事件至少需要包含：

```json
{
  "eventId": "evt-20260822-000002",
  "lineId": "LINE-01",
  "stageCode": "APPEARANCE_INSPECTION",
  "deviceCode": "VIS-REJECT-01",
  "traceCode": "BOTTLE-20260822-0001",
  "eventType": "REJECTED",
  "occurredAt": "2026-08-22T14:30:01+08:00",
  "reasonCode": "BODY_SCRATCH",
  "reason": "瓶身划痕",
  "source": "EDGE"
}
```

最终 Topic 和字段要以企业实际网关能力为准。如果企业已有 OPC UA、Modbus TCP、厂商 REST API 或其他协议，优先由企业网关统一转换，不要求三个鸿蒙应用理解这些协议。

### 5.3 控制和回执

控制链路必须是：

```text
工位端/后台/AI 意图
  -> Spring Boot 身份、范围、参数、版本、联锁校验
  -> 边缘控制器
  -> 设备执行
  -> 边缘回执
  -> Spring Boot 更新命令状态
  -> WebSocket/HTTP 通知前端和 AI
```

命令至少携带：

```json
{
  "clientRequestId": "terminal-20260822-0001",
  "commandId": "cmd-20260822-0001",
  "lineId": "LINE-01",
  "stageCode": "FILLING",
  "deviceCode": "FIL-PUMP-01",
  "commandType": "SET_FLOW_RATE",
  "parameters": {
    "value": 120,
    "unit": "ml/s"
  },
  "expectedStateVersion": 1024,
  "expectedParameterVersion": 18,
  "reason": "按已批准瓶型配方调整",
  "correlationId": "corr-20260822-0001"
}
```

企业边缘层回执至少需要返回：

```json
{
  "commandId": "cmd-20260822-0001",
  "clientRequestId": "terminal-20260822-0001",
  "deviceCode": "FIL-PUMP-01",
  "status": "ACKNOWLEDGED",
  "edgeAckId": "edge-ack-0001",
  "message": "设备已执行",
  "occurredAt": "2026-08-22T14:30:05+08:00"
}
```

状态必须区分：`PENDING`、`SENT`、`ACKNOWLEDGED`、`FAILED`、`TIMEOUT`、`CANCELLED`。企业必须提供设备拒绝、联锁阻止、超时、急停和离线时的回执或错误码。

## 6. 设备和数据要求

### 6.1 所有设备的通用字段

每个设备至少需要建立以下档案：

| 字段 | 要求 |
| --- | --- |
| `deviceCode` | 稳定、唯一、不可因页面显示名变化而改变 |
| `deviceName` | 中文显示名 |
| `deviceType` | 传感器、相机、泵、输送机、机械臂、AGV 等 |
| `stageCode` | 所属工序代码 |
| `lineId` | 所属产线 |
| `vendor/model` | 厂家和型号 |
| `protocol` | MQTT、OPC UA、Modbus TCP、HTTP 等 |
| `firmwareVersion` | 固件或驱动版本 |
| `capabilityVersion` | 可控参数和动作版本 |
| `calibrationDueAt` | 校准到期时间 |
| `safetyLevel` | 普通、受限、高风险等，由双方确认 |
| `online/state` | 在线状态和运行状态 |

### 6.2 传感器数据

企业需要给出每个传感器的点位表，至少包含：

| 点位字段 | 示例 | 企业填写 |
| --- | --- | --- |
| 设备编码 | `GAS-VERIFY-01` | 待确认 |
| 测量类型 | `VOC` | 待确认 |
| 原始地址/寄存器 | `40001` 或厂商 Tag | 待确认 |
| 数据类型 | `DOUBLE` | 待确认 |
| 单位 | `ppm` | 待确认 |
| 量程 | `0-100 ppm` | 待确认 |
| 精度 | `±0.1 ppm` | 待确认 |
| 采样周期 | `1 s` | 待确认 |
| 上报周期 | `1 s` | 待确认 |
| 质量码 | `GOOD/BAD/UNCERTAIN` | 待确认 |
| 校准周期 | 待确认 | 待确认 |
| 越限策略 | 报警/停机/隔离 | 待确认 |

重点数据范围：

- VOC/有害气体：气体种类、浓度、单位、采样舱状态、采样时间和校准状态。
- 温度/湿度：饮料温度、环境湿度、测量位置和杀菌阶段。
- 流量/液位：瞬时值、累计值、单位、允许范围和瓶子追踪号。
- 外观视觉：相机编码、触发号、瓶型、缺陷类别、置信度、图片引用和模型版本。
- AGV：车辆编码、任务号、速度、已行驶距离、障碍距离、负载、位置和任务状态。
- 仓储安全：VOC、烟雾、温度、区域编码、报警级别和库位状态。

### 6.3 视觉系统

企业需要在以下方案中确认一种：

1. 企业提供视觉识别结果接口，项目组接收结构化结果。
2. 企业提供相机图片/视频流和触发接口，项目组接入现有 AI 视觉服务。
3. 企业提供相机和推理服务器，由双方共同部署模型服务。

无论采用哪种方案，结果必须包含：

```text
inferenceId
cameraCode
lineId / stageCode
traceCode 或触发号
capturedAt
bottleTypeCode
defects[]
confidence
evidenceRef
modelVersion
status
latencyMs
```

瓶盖、瓶身、瓶底缺陷必须提供双方确认的标签字典，例如缺盖、歪盖、密封异常、划痕、凹陷、黑点、变形、瓶底缺陷等；不能只返回“合格/不合格”而没有原因。

### 6.4 AGV 和仓储

AGV 需要明确：

- 任务创建、取消、暂停、恢复和完成接口。
- 车辆状态、位置坐标系、路线编号和定位精度。
- 超声波障碍距离、避障阈值、急停状态和恢复条件。
- 负载检测范围、单位和超载处理。
- 多车冲突、优先级、重调度和通信中断策略。

WMS 需要明确：

- 箱码、产品追踪号、瓶型、数量和批次的绑定规则。
- 仓区、库位、入库、移库、出库和盘点接口。
- 仓区 VOC、烟雾、温度报警对入库和出库的影响。
- WMS 是主数据源还是本项目后端是主数据源，禁止双方同时无规则写同一库存。

## 7. 需要企业确认的关键参数

以下内容不能由项目组自行猜测：

| 类别 | 必须确认的内容 |
| --- | --- |
| 产线标识 | 产线数量、`lineId`、工序编码、设备归属 |
| 瓶型 | 瓶型编码、长宽高、重量、瓶口/瓶盖规格、允许混线规则 |
| PLA 预处理 | 冷却/静置时间、水洗和风洗时长、气体安全阈值 |
| 外观质量 | 缺陷类别、判定规则、置信度门槛、剔除时机和剔除失败策略 |
| 饮料工艺 | 配方版本、混合比例、杀菌温度/时长、温湿度范围 |
| 灌装工艺 | 灌装量、液位范围、流量范围、换型参数和联锁 |
| 装箱规则 | 箱体尺寸、允许排列方向、每箱数量、夹爪限制和码制 |
| AGV | 车辆数量、载重、速度、避障距离、地图、定位和调度 API |
| 仓储 | 仓区容量、库位编码、库存主数据、消防/气体阈值和 WMS 接口 |
| 网络 | IP、端口、VLAN、防火墙、TLS、证书、时间同步和远程访问 |
| 数据留存 | 遥测留存时长、图片留存时长、审计留存时长、备份和恢复要求 |
| 性能 | 采集频率、端到端延迟、允许丢包率、断线缓存时长和并发设备数 |

## 8. 企业交付资料模板

企业可以直接按以下清单回传文件。文件名和版本号要固定，后续修改必须有变更记录。

```text
01_设备清单.xlsx
02_PLC与边缘网关说明.pdf
03_设备点位表.xlsx
04_控制命令与回执表.xlsx
05_MQTT或企业接口说明.pdf
06_视觉相机与缺陷标签说明.pdf
07_AGV接口说明.pdf
08_WMS接口说明.pdf
09_网络拓扑与防火墙申请.xlsx
10_安全联锁与急停说明.pdf
11_工艺参数与质量阈值.xlsx
12_测试样品与故障注入说明.xlsx
```

### 8.1 设备点位表最小格式

| lineId | stageCode | deviceCode | tagCode | tagName | direction | dataType | unit | range | periodMs | qualityCode | remark |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | ---: | --- | --- |
| LINE-01 | FILLING | FIL-FLOW-01 | FLOW_RATE | 灌装流量 | READ | DOUBLE | ml/s | 待确认 | 待确认 | GOOD/BAD | 待确认 |

### 8.2 命令表最小格式

| deviceCode | commandType | parameterCode | direction | valueType | unit | min | max | step | requiredRole | interlock | ackTimeoutMs | errorCodes |
| --- | --- | --- | --- | --- | --- | ---: | ---: | ---: | --- | --- | ---: | --- |
| FIL-PUMP-01 | SET_FLOW_RATE | FLOW_RATE | WRITE | DOUBLE | ml/s | 待确认 | 待确认 | 待确认 | ENGINEER | 设备在线且未人工锁定 | 待确认 | 待确认 |

### 8.3 缺陷标签表最小格式

| defectCode | 中文名称 | 所属部位 | 严重等级 | 是否剔除 | 置信度门槛 | 图片证据要求 |
| --- | --- | --- | --- | --- | ---: | --- |
| BODY_SCRATCH | 瓶身划痕 | 瓶身 | 待确认 | 待确认 | 待确认 | 是 |

## 9. 联调和验收计划

### 阶段 0：资料和环境确认

- 设备清单、点位表、命令表、协议、安全和网络资料齐全。
- 企业提供测试 Broker、测试账号、测试设备或可重复模拟器。
- 双方确认 `lineId`、`stageCode`、`deviceCode`、单位、时间格式和状态枚举。

### 阶段 1：单设备接入

选择一个温度、VOC 或 AGV 超声波设备完成：

1. 设备上线和离线状态。
2. 遥测数据接收、单位和质量码验证。
3. 时间戳、序列号、重复消息和乱序消息验证。
4. 断网缓存、重连补发和异常数据验证。
5. 后端数据库保存和 API 查询验证。

### 阶段 2：单工序闭环

选择气体检测、外观检测或灌装工序完成：

1. 设备数据进入后端。
2. 产品追踪号与设备事件绑定。
3. 质量门判定进入后端。
4. 不合格品剔除事件和原因可追踪。
5. 工位端和数字展板实时显示同一状态。
6. 控制命令经过校验后到达设备，并收到明确回执。

### 阶段 3：AI 链路

使用一个已确认瓶型和一套已审核知识资料验证：

```text
视觉结果
 -> AI 获取实时事实
 -> RAG 检索已审核知识
 -> 数据库/后端校验配方、阈值、能力和人工锁
 -> 生成字段级建议
 -> 后端安全闸口
 -> 边缘执行
 -> 回执
 -> 三端展示决策和结果
```

AI 问答必须验证“只读”，普通提问不能产生设备命令；隐式决策必须能展示知识引用、实时数据版本、被人工锁定字段和最终回执。

### 阶段 4：整线和异常

至少演练：

- 一台设备离线。
- 一个传感器越限。
- 相机识别低置信度。
- 剔除机构执行失败。
- 灌装设备参数版本变化。
- 人工锁定参数后 AI 建议被阻止。
- AGV 遇障、超载、失联和重新调度。
- 仓区烟雾/VOC 报警。
- 后端、AI 中枢、MQTT Broker 分别重启。
- 同一消息重复发送、乱序发送和延迟回执。

### 阶段 5：前端和交付

- 三个鸿蒙应用使用真实服务地址运行。
- 数字展板 2D/3D 与实时数据一致，暂停后恢复跳到当前状态。
- 工位端只显示本站权限，控制结果与设备回执一致。
- 后台能查询设备、质量、异常、AI 决策、知识和审计记录。
- 断线时显示离线/模拟状态，不显示假成功。
- 完成连续运行、内存、网络恢复和日志检查。

## 10. 验收判定

除双方另行书面确认外，以下情况不得判定为“已打通”：

1. 只有页面显示模拟数据，没有真实设备来源标记。
2. 只有 HTTP 返回成功，没有设备执行回执。
3. 只有“合格/不合格”，没有瓶型、缺陷类别、阈值或证据引用。
4. 数据没有设备时间、单位、质量码或稳定业务 ID。
5. 设备断线后前端仍显示绿色运行。
6. AI 直接写 MySQL、直接访问 PLC 或绕过后端下发命令。
7. 企业设备编码与项目设备编码没有映射表。
8. 现场安全联锁只依赖软件页面按钮，没有硬件/边缘层保护。
9. WMS、AGV 或视觉服务没有明确的主数据和故障责任边界。

建议将验收证据保存为：接口请求/响应、MQTT 报文、设备日志、后端日志、数据库记录、前端截图、命令回执和异常演练记录，并关联统一的 `eventId`、`traceCode` 或 `correlationId`。

## 11. 当前项目还需要补充的工作

企业接入前，项目组还需要安排以下开发任务：

| 优先级 | 工作项 | 原因 |
| --- | --- | --- |
| P0 | 根据企业点位表实现设备编码和 Tag 映射 | 当前代码只有示例设备，不能直接匹配现场设备 |
| P0 | 冻结 MQTT/边缘网关协议和命令回执适配器 | 当前只具备演示接入基础，真实命令 Topic 尚需确认 |
| P0 | 接入真实阈值、配方、设备能力和人工锁数据 | AI 决策不能使用示例阈值替代生产规则 |
| P0 | 完成视觉结果接收和瓶型/缺陷标签映射 | 真实相机、模型和剔除回执尚未联调 |
| P0 | 完成端到端产品追踪号绑定 | 没有 `traceCode` 就无法关联检测、灌装、装箱和入库 |
| P1 | 接入 AGV 厂商任务和状态接口 | 当前业务模型有 AGV 数据字段，尚需对接车辆系统 |
| P1 | 接入 WMS 主数据和入库接口 | 当前有仓储业务接口，尚需确认库存主数据归属 |
| P1 | 补充真实 MQTT 安全、重连、断线缓存和重复消息测试 | 演示环境不能代表现场网络 |
| P1 | 接入生产级向量库、文档解析和正式 MCP 工具 | 当前 RAG/MCP 仍属于演示或待接入能力 |
| P1 | 完成模拟器/真机与企业局域网的安装验收 | 需要验证 ArkWeb、WebSocket、长时间运行和网络恢复 |

## 12. 待双方签字确认

在企业开始安装或联调前，请确认以下内容：

```text
产线名称：____________________________
产线数量：____________________________
企业项目负责人：______________________
企业自动化负责人：____________________
企业 IT/网络负责人：___________________
项目组负责人：________________________
计划开始联调日期：____________________
计划完成验收日期：____________________

MQTT/边缘网关方案：___________________
视觉方案：____________________________
AGV 方案：_____________________________
WMS 方案：_____________________________
数据留存要求：________________________
安全阈值版本：________________________
接口文档版本：________________________
```

任何现场参数、协议、设备编码或安全规则发生变化，都必须更新本文档、点位表和版本记录，并重新执行受影响的验收项。

## 13. 相关项目文档

- [README.md](README.md)：项目组成、边界和当前能力。
- [ONBOARDING-DEPLOYMENT.md](ONBOARDING-DEPLOYMENT.md)：软件安装、部署、启动和模拟联调。
- [contracts/api-contract.md](contracts/api-contract.md)：HTTP 接口和 AI 集成接口。
- [contracts/realtime-contract.md](contracts/realtime-contract.md)：WebSocket 实时事件和断线补偿。
- [contracts/domain-contract.md](contracts/domain-contract.md)：设备、产品、传感器、命令和质量对象。
- [contracts/control-security-contract.md](contracts/control-security-contract.md)：身份、参数、人工锁、联锁和命令回执。
- [contracts/ai-decision-contract.md](contracts/ai-decision-contract.md)：视觉、RAG、实时校验和 AI 决策闭环。
