# 项目实际施工入口

这份文件是当前代码的真实运行入口。每完成一组步骤就记录结果，遇到失败先停在当前步骤。

## 当前已经完成的代码

~~~text
Spring Boot factory 模块
正常瓶、气体超限、外观缺陷、二检缺陷、AGV阻塞场景
严格状态机和跳步拒绝
REST API
管理端产线演示页面
数字大屏产线总览页面
可选 MQTT Bridge
Node MQTT 设备模拟器
factory_run / factory_event 持久化表和 JDBC 仓储
运行中心：传感器、报警、设备命令/ACK、AI审计
装箱网格坐标计算
AGV任务、超声波避障、仓储安全和箱级库存
Python固定场景视觉服务
本地RAG HTTP服务与MCP只读工具服务
鸿蒙ArkTS工位端模板（待DevEco Studio门禁）
~~~

真实摄像头模型、机械臂厂商协议、AGV厂商协议和鸿蒙HAP编译仍需在对应设备/开发机上验证；当前代码明确标记为 SIMULATION，不把模拟结果伪装成真实设备结果。

## 零基础第一次启动顺序

所有依赖、缓存和运行数据优先放在 E 盘。先打开 5 个 PowerShell 窗口，窗口不要互相覆盖。

~~~powershell
# 窗口1：E盘 MySQL 9.6，首次运行会初始化 .runtime\mysql-data\data
Set-Location 'E:\2026挑战杯\工业沙盘2026\数字展板前后端'
& '.\.runtime\scripts\setup-mysql.ps1'

# 窗口2：MQTT Broker
& '.\.runtime\scripts\run-broker.ps1'

# 窗口3：本地视觉服务，8090
& '.\.runtime\scripts\run-vision.ps1'

# 窗口4：本地 RAG AI 服务，8091
& '.\.runtime\scripts\run-ai.ps1'

# 窗口5：正式 dev 后端，8088
& '.\.runtime\scripts\run-backend-dev.ps1'
~~~

第 5 个窗口显示 Spring Boot 启动后，再开第 6 个窗口执行：

~~~powershell
Set-Location 'E:\2026挑战杯\工业沙盘2026\数字展板前后端'
& '.\.runtime\scripts\verify-core.ps1'
~~~

必须看到 `FactoryStatus=COMPLETED`、`EventCount=10`、`AiValidation=PASSED`、`CommandStatus=SUCCEEDED`、`PackingCount=32`、`AgvStatus=ARRIVED`、`WarehouseStatus=IN_STOCK`。

如果只是学习后端状态机，不想启动 MySQL/MQTT，使用 `run-backend-demo.ps1`，此模式数据只在内存中，不用于持久化验收。

## 第一个门禁：编译和测试

~~~powershell
Set-Location 'E:\2026挑战杯\工业沙盘2026\数字展板前后端\数字大屏后端code\hdc_server'
$mvn='E:\dev\apache-maven-3.9.9\bin\mvn.cmd'
& $mvn '-o' '-Dmaven.repo.local=E:/dev/maven-repository' 'test'
& $mvn '-o' '-Dmaven.repo.local=E:/dev/maven-repository' '-DskipTests' 'package'
~~~

预期结果：

~~~text
Tests run: 9, Failures: 0, Errors: 0
BUILD SUCCESS
target\hdc-2024031501.jar 存在
~~~

两个前端：

~~~powershell
Set-Location 'E:\2026挑战杯\工业沙盘2026\数字展板前后端\数字大屏前端code\hdc_web'
npm run build
Set-Location '..\hdc_digitallargescreen_web'
npm run build
~~~

## 第二个门禁：不依赖数据库的 REST 演示

打开第一个 PowerShell：

~~~powershell
Set-Location 'E:\2026挑战杯\工业沙盘2026\数字展板前后端\数字大屏后端code\hdc_server'
java -jar '.\target\hdc-2024031501.jar' '--spring.profiles.active=factory-demo'
~~~

另开窗口健康检查：

~~~powershell
Invoke-RestMethod 'http://127.0.0.1:8088/hdc/api/factory/health' | ConvertTo-Json -Depth 5
~~~

必须看到 status=UP、mode=SIMULATION、service=factory-core。

执行三种场景：

~~~powershell
$base='http://127.0.0.1:8088/hdc/api/factory/runs/scenario'
Invoke-RestMethod $base -Method Post -ContentType 'application/json' -Body '{"traceCode":"BOT-REST-001","bottleType":"PLA-500","scenario":"NORMAL"}'
Invoke-RestMethod $base -Method Post -ContentType 'application/json' -Body '{"traceCode":"BOT-REST-002","bottleType":"PLA-500","scenario":"GAS_ALARM"}'
Invoke-RestMethod $base -Method Post -ContentType 'application/json' -Body '{"traceCode":"BOT-REST-003","bottleType":"PLA-500","scenario":"APPEARANCE_DEFECT"}'
~~~

预期：

~~~text
BOT-REST-001：COMPLETED，生成箱码、AGV任务和库位
BOT-REST-002：HOLD，停在 GAS_INSPECTION，VOC=12.5
BOT-REST-003：REJECTED，缺陷 BODY_DENT
~~~

## 第三个门禁：启动管理端和大屏

管理端：

~~~powershell
Set-Location 'E:\2026挑战杯\工业沙盘2026\数字展板前后端\数字大屏前端code\hdc_web'
npm run serve
~~~

打开 http://localhost:8080/#/factory，点击正常瓶、气体超限、外观缺陷。

大屏：

~~~powershell
Set-Location 'E:\2026挑战杯\工业沙盘2026\数字展板前后端\数字大屏前端code\hdc_digitallargescreen_web'
npm run serve -- --port 8081
~~~

打开 http://localhost:8081/#/factoryOverview。大屏每 2 秒查询 dashboard，后端停止时应显示后端断开。

## 第四个门禁：MQTT 设备模拟

启动 Broker：

~~~powershell
Set-Location 'E:\2026挑战杯\工业沙盘2026\数字展板前后端\.runtime\mqtt-broker'
node .\start-broker.js
~~~

启动 MQTT 版本后端：

~~~powershell
Set-Location 'E:\2026挑战杯\工业沙盘2026\数字展板前后端\数字大屏后端code\hdc_server'
java -jar '.\target\hdc-2024031501.jar' '--spring.profiles.active=factory-mqtt-demo'
~~~

创建批次并运行模拟器：

~~~powershell
Invoke-RestMethod 'http://127.0.0.1:8088/hdc/api/factory/runs' -Method Post -ContentType 'application/json' -Body '{"traceCode":"BOT-MQTT-001","bottleType":"PLA-500","scenario":"MANUAL"}'
Set-Location 'E:\2026挑战杯\工业沙盘2026\数字展板前后端\.runtime\factory-simulator'
npm install --cache 'E:\dev\npm-cache'
$env:TRACE_CODE='BOT-MQTT-001'
npm start
~~~

查询结果：

~~~powershell
Invoke-RestMethod 'http://127.0.0.1:8088/hdc/api/factory/runs/BOT-MQTT-001' | ConvertTo-Json -Depth 8
~~~

必须看到 COMPLETED、箱码、AGV任务和库位。这一步证明 MQTT 消息已经进入状态机。

## 第五个门禁：E盘 MySQL 持久化

项目已经准备了 E 盘 MySQL 9.6，不需要 Docker。`E:\hdc_project` 只是英文路径联接，用来规避 MySQL Windows 版读取中文目录时的乱码；真实文件仍在当前项目的 `.runtime` 目录。MySQL 监听 3307，数据库名 `hdc`，演示密码 `123456`。

~~~powershell
java -jar '.\target\hdc-2024031501.jar' '--spring.profiles.active=dev'
~~~

验证：

~~~powershell
$mysql='E:\hdc_project\.runtime\mysql\mysql-9.6.0-winx64\bin\mysql.exe'
& $mysql '--protocol=tcp' '-h127.0.0.1' '-P3307' '-uroot' '-p123456' 'hdc' '-e' 'SHOW TABLES;'
~~~

应包含 `factory_run`、`factory_event`、`sensor_reading`、`factory_alarm`、`device_command`、`ai_decision_audit`、`agv_task`、`warehouse_zone`、`warehouse_stock`。重启后端前后查询同一个 `traceCode`，历史事件和 AI 审计仍存在，才算持久化门禁通过。

## 第六个门禁：视觉、AI、MCP

视觉服务：`http://127.0.0.1:8090/health`；AI 服务：`http://127.0.0.1:8091/health`。分别执行：

~~~powershell
& '.\.runtime\scripts\check-vision.ps1'
& '.\.runtime\scripts\check-ai-mcp.ps1'
~~~

视觉服务的 `NORMAL` 必须返回 `PASS`，`APPEARANCE_DEFECT` 必须返回 `REJECT/BODY_DENT`。AI 服务只读 `.runtime\ai-service\knowledge.json`；未知瓶型返回 `MANUAL_REVIEW`，禁止直接产生设备命令。MCP 当前只提供 `search_recipe` 和 `get_device_limits` 两个只读工具。

## 第七个门禁：前端和鸿蒙工位端

管理端地址为 `http://localhost:8080/#/factory`，大屏地址为 `http://localhost:8081/#/factoryOverview`。管理端会显示实时通道、传感器、报警、AI审计和命令 ACK；大屏会显示实时通道、报警条和传感器状态。

鸿蒙模板位于 `.runtime\harmony-workstation`。当前环境没有 DevEco Studio/鸿蒙 SDK，因此只能检查模板文件，不能在这台 Windows 环境宣称 HAP 编译成功。必须在鸿蒙开发机打开该目录，修改 `FactoryApi.ets` 中的局域网 IP，先以 SIMULATION 模式运行。

## 故障回退

~~~text
编译失败：先看第一个 ERROR，不修改前端
8088 不通：检查 Java 进程和日志
健康接口失败：确认 profile 是 factory-demo 或 factory-mqtt-demo
页面无数据：F12 → Network → 检查 /hdc/api/factory/dashboard
MQTT 无事件：检查 31883、主题、traceCode 和 stage
数据库失败：切回 factory-demo，先恢复软件演示，再查 3307、用户名密码和 .runtime 日志
8090/8091 不通：检查 Python 进程、端口和对应 `*.err.log`
装箱数量不对：先用相同尺寸调用 `/operations/packing/calculate`，确认单位都是 mm
AGV 入库失败：必须按 DISPATCHED → BLOCKED/ RUNNING → ARRIVED → IN_STOCK 顺序操作
鸿蒙无法请求：手机/平板和电脑必须同一局域网，不能使用 127.0.0.1
~~~

每次修复后重新执行当前门禁，不能用之前成功过代替本次验证。
