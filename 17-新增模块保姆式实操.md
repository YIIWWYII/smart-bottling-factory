# 新增模块保姆式实操

这节课对应当前已经写入项目的真实代码。先记住一条总规则：

> 设备只上报事实；后端负责状态机、阈值和安全校验；AI只给建议；设备命令必须等待 ACK；前端只展示和发起请求。

## 1. 先认识四个地址

| 地址 | 是什么 | 什么时候启动 |
|---|---|---|
| `8088` | Spring Boot 后端，唯一业务中枢 | 每次完整演示 |
| `8090` | Python 视觉合同服务 | 视觉演示 |
| `8091` | 本地 RAG AI 服务 | AI/RAG 演示 |
| `31883` | MQTT Broker，设备消息中转站 | MQTT 或 dev 模式 |

`8080` 是管理端，`8081` 是数字大屏，`3307` 是项目自己的 E 盘 MySQL。

第一次启动按项目根目录的 `00-项目实际施工入口.md` 执行。不要直接双击 JAR，因为看不到当前使用的 profile，也容易把日志弄丢。

## 2. 传感器上报

### 2.1 为什么先做统一合同

温度、湿度、VOC、烟雾、AGV 超声波的厂商不同，但业务只关心：设备编号、传感器类型、工序、数值、单位、质量、REAL/SIMULATION 模式。后端收到后写入 `sensor_reading`，再根据类型检查阈值。

### 2.2 手动上报一条安全温度

~~~powershell
$base='http://127.0.0.1:8088/hdc/api'
$body=@{
  deviceCode='TEMP-MIX-01'
  sensorType='TEMPERATURE'
  stage='BEVERAGE_READY'
  traceCode='BOT-LESSON-001'
  value=25.0
  unit='C'
  quality='GOOD'
  mode='SIMULATION'
} | ConvertTo-Json
Invoke-RestMethod "$base/operations/sensors/readings" -Method Post -ContentType 'application/json' -Body $body | ConvertTo-Json -Depth 6
~~~

看到响应 `code=0`，并且 `data.readingId` 不为空，说明接收成功。然后查询：

~~~powershell
Invoke-RestMethod "$base/operations/sensors/latest" | ConvertTo-Json -Depth 6
~~~

### 2.3 故意上报 VOC 超限

把上面的 `sensorType` 改成 `VOC`，`value=12.5`，`unit=ppm`。默认演示阈值是 `10.0 ppm`，所以后端会同时产生一条 `VOC_LIMIT` 报警。这个阈值只是演示配置，真实比赛或工厂必须由安全负责人确认，不能把演示值当成材料安全标准。

查询未确认报警：

~~~powershell
Invoke-RestMethod "$base/operations/alarms?status=OPEN" | ConvertTo-Json -Depth 8
~~~

确认报警：

~~~powershell
$alarm=(Invoke-RestMethod "$base/operations/alarms?status=OPEN").data[0]
Invoke-RestMethod "$base/operations/alarms/$($alarm.alarmId)/ack" -Method Post | ConvertTo-Json -Depth 8
~~~

确认接口是幂等的，重复确认不会再改变时间；未知报警编号会返回错误。

## 3. AI、RAG、MCP 和后端校验

### 3.1 调用本地 RAG 服务

AI 服务读取 `.runtime\ai-service\knowledge.json`。它不是魔法模型：先按 `bottleType` 找批准配方，再用 `observations` 检查 VOC/烟雾限制，返回配方和证据文件。

~~~powershell
$body=@{
  traceCode='BOT-LESSON-001'
  bottleType='PLA-500'
  stage='FILLING'
  observations=@{ voc=6.2; smoke=0.1 }
} | ConvertTo-Json -Depth 5
Invoke-RestMethod 'http://127.0.0.1:8091/decide' -Method Post -ContentType 'application/json' -Body $body | ConvertTo-Json -Depth 8
~~~

### 3.2 调用后端 AI 合同

正式页面和鸿蒙端应调用后端 `/operations/ai/decide`，而不是直接调用 AI 服务。原因是后端要记录 `ai_decision_audit`，并且只有后端验证通过才允许 `autoApply=true` 创建 `PENDING` 命令。

~~~powershell
$body=@{
  traceCode='BOT-LESSON-001'
  bottleType='PLA-500'
  stage='FILLING'
  observations=@{ voc=6.2 }
  autoApply=$true
} | ConvertTo-Json -Depth 5
$decision=Invoke-RestMethod "$base/operations/ai/decide" -Method Post -ContentType 'application/json' -Body $body
$decision | ConvertTo-Json -Depth 8
~~~

必须检查：`validationStatus=PASSED`、`decision=APPLY_RECIPE`、`commandId` 不为空。未知瓶型如 `UNKNOWN-999` 必须是 `MANUAL_REVIEW`，没有命令。

### 3.3 模拟 PLC ACK

设备真正接入时，PLC/控制器完成参数写入后发布 ACK；学习阶段手动模拟：

~~~powershell
$commandId=$decision.data.commandId
Invoke-RestMethod "$base/operations/commands/$commandId/ack" -Method Post -ContentType 'application/json' `
  -Body (@{ status='SUCCEEDED'; message='PLC recipe loaded' } | ConvertTo-Json) | ConvertTo-Json -Depth 8
~~~

命令状态顺序是 `PENDING → SUCCEEDED` 或 `PENDING → FAILED`。已经 ACK 的命令不能再次 ACK，避免重复执行。

### 3.4 MCP 实操

MCP 服务是只读工具层，不是设备控制层。执行 `check-ai-mcp.ps1` 会发送 `initialize`、`tools/list` 和 `tools/call` 三个 JSON-RPC 消息。团队学习时观察 `tools/list` 中的两个工具：`search_recipe`、`get_device_limits`。后续接入模型时，模型只能调用这些工具拿证据，最终仍回到后端校验。

## 4. 视觉服务

### 4.1 先验证服务

~~~powershell
Invoke-RestMethod 'http://127.0.0.1:8090/health' | ConvertTo-Json
& '.\.runtime\scripts\check-vision.ps1'
~~~

### 4.2 发正常瓶和缺陷瓶

~~~powershell
$body=@{ traceCode='BOT-V-001'; bottleType='PLA-500'; scenario='NORMAL'; mode='SIMULATION' } | ConvertTo-Json
Invoke-RestMethod 'http://127.0.0.1:8090/infer' -Method Post -ContentType 'application/json' -Body $body | ConvertTo-Json -Depth 8

$body=@{ traceCode='BOT-V-002'; bottleType='PLA-500'; scenario='APPEARANCE_DEFECT'; mode='SIMULATION' } | ConvertTo-Json
Invoke-RestMethod 'http://127.0.0.1:8090/infer' -Method Post -ContentType 'application/json' -Body $body | ConvertTo-Json -Depth 8
~~~

正常结果是 `PASS`，缺陷结果是 `REJECT` 且缺陷类型 `BODY_DENT`。当前 `infer()` 是固定场景模拟器；真实视觉负责人以后只替换该函数：读取摄像头/图片、调用模型、把输出转换成同样的 `decision/defects/confidence` JSON。没有通过真实图片测试前，页面必须显示 `SIMULATION`。

## 5. 装箱计算

接口：`POST /operations/packing/calculate`。所有尺寸单位都是毫米。

~~~powershell
$body=@{
  bottleType='PLA-500'
  bottleWidthMm=70; bottleLengthMm=70; bottleHeightMm=200
  boxWidthMm=300; boxLengthMm=300; boxHeightMm=450
  allowRotation=$true
} | ConvertTo-Json
$packing=Invoke-RestMethod "$base/operations/packing/calculate" -Method Post -ContentType 'application/json' -Body $body
$packing | ConvertTo-Json -Depth 8
~~~

计算逻辑是：

1. 箱宽除以瓶宽得到列数。
2. 箱长除以瓶长得到行数。
3. 箱高除以瓶高得到层数。
4. 如果允许旋转，再计算瓶宽/瓶长互换后的数量，取较大值。
5. 输出每个瓶子的 `xMm/yMm/zMm` 中心坐标和旋转角度。

这个算法只负责规则网格，真实机械臂还必须加抓取高度、夹爪宽度、箱内缓冲、碰撞检测、坐标系标定和急停。确认这些条件前，禁止把坐标转换成 REAL 机械臂指令。

## 6. AGV 和仓储

### 6.1 创建任务

~~~powershell
$task=Invoke-RestMethod "$base/logistics/agv/tasks" -Method Post -ContentType 'application/json' `
  -Body (@{ boxCode='BOX-LESSON-001'; source='PACKING'; destination='A-01'; totalDistanceM=10; loadKg=5; mode='SIMULATION' } | ConvertTo-Json)
$task | ConvertTo-Json -Depth 8
~~~

### 6.2 模拟超声波避障

发送 `obstacleDistanceCm=10`，必须返回 `BLOCKED`，速度被后端置为 0：

~~~powershell
Invoke-RestMethod "$base/logistics/agv/tasks/$($task.data.taskId)/telemetry" -Method Post -ContentType 'application/json' `
  -Body (@{ speedMps=1; completedDistanceM=2; obstacleDistanceCm=10; loadKg=5 } | ConvertTo-Json)
~~~

发送距离恢复到 100cm 且里程到 10m，返回 `ARRIVED`：

~~~powershell
Invoke-RestMethod "$base/logistics/agv/tasks/$($task.data.taskId)/telemetry" -Method Post -ContentType 'application/json' `
  -Body (@{ speedMps=1; completedDistanceM=10; obstacleDistanceCm=100; loadKg=5 } | ConvertTo-Json)
~~~

### 6.3 仓储安全和入库

~~~powershell
Invoke-RestMethod "$base/logistics/warehouse/zones/A-01/telemetry" -Method Post -ContentType 'application/json' `
  -Body (@{ vocPpm=3; smoke=0.1; temperatureC=25 } | ConvertTo-Json)

Invoke-RestMethod "$base/logistics/warehouse/inbound" -Method Post -ContentType 'application/json' `
  -Body (@{ taskId=$task.data.taskId; boxCode='BOX-LESSON-001'; bottleType='PLA-500'; zoneCode='A-01'; locationCode='A-01-01' } | ConvertTo-Json)
~~~

仓区烟雾大于 `0.5` 或 VOC 大于 `10` 时状态是 `ALARM`，此时入库会被拒绝。入库顺序不可跳过：AGV 必须先 `ARRIVED`，仓区必须 `SAFE`，最后库存状态才是 `IN_STOCK`。

## 7. 前端观察点

1. 管理端打开 `http://localhost:8080/#/factory`。
2. 点击“气体超限”，看批次进入 `HOLD`，运行中心出现报警。
3. 点击“正常瓶”，看 AI 审计数量增加，命令状态从 `PENDING` 到 `SUCCEEDED` 的演示数据可在运行中心看到。
4. 大屏打开 `http://localhost:8081/#/factoryOverview`，看实时通道和报警条。
5. 关闭后端窗口，页面应显示断开；重新启动后，轮询会恢复。

F12 调试顺序固定为：`Network` 看 URL 和状态码，`Response` 看 `code/message/data`，后端窗口看异常栈，最后才检查数据库。不要一开始就改前端地址。

## 8. 真实设备替换顺序

1. 先保持所有模块 `SIMULATION`，连续跑通 `verify-core.ps1`。
2. 用一台真实传感器替换一个模拟上报器，只验证数值和单位。
3. 再替换摄像头，只验证图片上传和识别结果。
4. 再替换 PLC 命令适配器，只验证 `PENDING/ACK/FAILED`。
5. 最后才接机械臂、AGV 和鸿蒙工位端。

任何真实设备型号、串口协议、Modbus 寄存器、机械臂坐标系都必须从设备说明书确认。没有这些资料时，代码中的 `REAL` 只能作为合同字段，不能直接打开。
