# 第十三课：鸿蒙工位控制系统

## 本节学习路线

### 这是什么

这是鸿蒙工位屏的完整控制闭环：查询状态、提交HTTP命令、后端校验、MQTT发送、设备ACK、WebSocket返回最终结果。

### 为什么要学

点击按钮后变绿不代表设备真的执行。必须把受理、发送、执行和失败分成可追踪状态，才能安全控制产线。

### 它在项目中的位置

鸿蒙页面 → ApiService HTTP → Spring Boot校验 → MQTT command → 设备ACK → Spring Boot状态更新 → WebSocket → 鸿蒙页面。

### 需要的软件和配置

- DevEco Studio。
- 鸿蒙模拟器或真机。
- Spring Boot、MQTT Broker和Node设备模拟器。
- PowerShell验证局域网IP和端口。

鸿蒙设备不能用localhost访问开发电脑。先用ipconfig找服务器IP，保证两者在同一局域网，并开放8088和必要端口。

### 最小例子

固定WASH-01和PUMP-01。鸿蒙请求快照，点击启动，收到PENDING，再由模拟器返回ACK，页面最后显示SUCCESS。

### 项目实操顺序

1. 在电脑执行ipconfig并确定服务器IP。
2. 用PowerShell验证8088可访问。
3. ApiService请求stations/WASH-01/snapshot。
4. 解析响应并绑定StationStatus。
5. 提交station-commands。
6. 保存返回的commandId。
7. 连接station/WASH-01 WebSocket。
8. 只处理匹配commandId的事件。
9. 启动第7课Node模拟器返回ACK。
10. 测试SUCCESS、FAILED、TIMEOUT和后端REJECTED。
11. 测试断线后先快照再事件。
12. 给停线、放行和清除报警增加二次确认。

### 鸿蒙控制调试怎么做

- HTTP失败：先在PowerShell调用同一URL，区分服务器问题和鸿蒙网络问题。
- 响应字段为空：打印原始响应，再核对JSON字段名。
- 页面显示成功但设备没动作：查MQTT command和设备ACK。
- 收到错误命令成功：检查commandId匹配。
- 一直加载：处理HTTP异常、WebSocket关闭和超时。
- 真机访问不了：查局域网、防火墙和服务器监听地址。

### 如何与下一节连接

第14课视觉服务的检测结果也会通过后端进入鸿蒙屏；第15课AI建议在管理端审核后，仍通过同一命令闭环；第16课把真实设备逐个替换模拟器。

### 通过门禁

鸿蒙屏能完整展示HTTP、MQTT、ACK、WebSocket链路，后端拒绝、设备失败、超时、断线和恢复都有清晰页面结果。

## 学习目标

- 用HTTP查询工位快照和提交控制命令。
- 用WebSocket接收命令执行结果和报警。
- 区分命令受理、发送、ACK、成功、失败和超时。
- 处理自动、手动、维修模式和危险操作。
- 处理局域网地址、断线重连、应用生命周期和权限。

## 鸿蒙端分层

```text
页面层：ArkUI页面和按钮
服务层：HTTP与WebSocket
模型层：工位、传感器、命令结构
状态层：当前工位与网络状态
```

目录：

```text
pages/WatchStationPage.ets
components/DeviceStatusCard.ets
components/AlarmBanner.ets
model/StationStatus.ets
model/CommandResult.ets
service/ApiService.ets
service/WebSocketService.ets
```

页面不直接堆放网络连接和JSON解析。

## 工位接口

```text
GET  /hdc/api/stations/{stationCode}/snapshot
GET  /hdc/api/stations/{stationCode}/status
POST /hdc/api/station-commands
POST /hdc/api/alarms/{alarmId}/ack
```

快照响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "stationCode": "WASH-01",
    "state": "IDLE",
    "mode": "MANUAL",
    "deviceOnline": true,
    "currentStep": "PRETREATMENT",
    "currentBottleCode": "BOT-0001",
    "gasValue": 6.2,
    "gasThreshold": 10,
    "allowedActions": ["START_WASH", "STOP_LINE"],
    "updatedAt": "2026-07-19T15:00:00+08:00"
  }
}
```

## HTTP服务要点

- 使用当前HarmonyOS API版本对应的NetworkKit HTTP能力。
- 设置连接和读取超时。
- 每次请求后释放HTTP对象。
- 统一处理业务码、未登录、无权限、超时和网络错误。
- 服务器地址放配置，不散落在页面。

重要网络规则：

- 鸿蒙真机中的`localhost`指鸿蒙设备自己。
- 真机访问电脑后端必须使用电脑局域网IP。
- 设备与服务器处于同一局域网。

## 控制请求

```json
{
  "stationCode": "WASH-01",
  "deviceCode": "PUMP-01",
  "action": "START",
  "parameters": {
    "durationSeconds": 15
  }
}
```

后端受理：

```json
{
  "code": 0,
  "message": "命令已受理",
  "data": {
    "commandId": "CMD-20260719-0001",
    "status": "PENDING"
  }
}
```

此时只能显示“命令已受理”，不能显示“水洗完成”。

## 命令状态

- `PENDING`：等待发送。
- `SENT`：已发送MQTT。
- `ACKED`：设备已收到。
- `EXECUTING`：执行中。
- `SUCCESS`：执行成功。
- `FAILED`：执行失败。
- `TIMEOUT`：执行超时。
- `REJECTED`：后端拒绝。

页面保存：

```ts
@State commandStatus: string = 'NONE'
@State commandMessage: string = ''
@State commandId: string = ''
```

## 完整控制流程

```text
点击启动
→ 本地检查按钮条件
→ HTTP提交
→ 页面显示PENDING
→ 后端校验并发布MQTT
→ 设备ACK
→ WebSocket推送状态
→ 匹配commandId
→ 页面显示最终结果
```

HTTP失败时显示网络或业务原因；不能在失败后自动连续重复发送危险命令。

## WebSocket事件

```json
{
  "eventId": "WS-0001",
  "type": "command.status.changed",
  "stationId": "WASH-01",
  "data": {
    "commandId": "CMD-20260719-0001",
    "status": "SUCCESS",
    "message": "水洗完成"
  }
}
```

处理原则：

- `commandId`必须与当前命令匹配。
- `stationId`必须属于当前工位。
- `station.snapshot.changed`更新工位快照。
- `alarm.created`显示报警。
- 旧版本和重复事件忽略。

## WebSocket服务职责

- 创建连接。
- 处理消息和心跳。
- 处理错误和关闭。
- 递增间隔重连。
- 应用退出时清理连接和计时器。
- 页面只接收整理后的业务事件。

## 断线处理

断线时：

- 顶部显示实时连接断开。
- 不把旧状态继续伪装为实时。
- 显示最后更新时间。
- 谨慎禁用控制按钮。
- 提供重连入口。

重连后：

```text
先HTTP查询完整快照
→ 再继续接收WebSocket事件
```

## 后端二次校验

鸿蒙端可以禁用按钮，但Spring Boot仍检查：

- 用户权限。
- 工位状态和运行模式。
- 瓶子位置与前序工序。
- 设备在线状态。
- 活动命令。
- 安全参数和报警。

前端条件不能代替安全控制。

## 危险操作二次确认

需要确认：

- 停止整线。
- 手动放行。
- 清除报警。
- 修改安全阈值。
- 启动机械臂。
- 启动AGV。

确认内容包括对象、动作和影响。确认后仍由后端决定是否执行。

## 运行模式

### AUTO

- 后端流程自动推进。
- 普通控制按钮隐藏或禁用。
- 授权人员仍可紧急停止。

### MANUAL

- 操作员通过工位屏提交允许的控制请求。

### MAINTENANCE

- 维修调试使用。
- 不能关闭基础安全和急停逻辑。

## 报警处理

报警消息包含：

```text
alarmId
level
stationCode
message
value
threshold
unit
occurredAt
```

页面显示报警横幅、当前值、阈值和单位，并禁用受影响工序。

- 确认报警：有人看到并接手。
- 解除报警：实际问题恢复。
- 两者必须分开。

## 命令超时

```text
提交命令
→ 规定时间未收到ACK
→ 状态TIMEOUT
→ 显示设备未响应
→ 禁止自动重复发送
```

机械臂、灌装机和AGV尤其不能因网络慢重复启动。

## 局域网配置

示例：

```text
后端电脑：192.168.1.100:8088
鸿蒙设备：同网段
SERVER_URL：http://192.168.1.100:8088/hdc
```

- 演示前固定服务器IP。
- 可以增加服务器地址配置页，避免每次重新打包。
- Windows防火墙允许8088和WebSocket连接。

## 实操作业

- 创建`ApiService`。
- 配置局域网后端地址。
- 查询`WASH-01`快照。
- 提交启动水洗请求。
- 显示`commandId`与`PENDING`。
- 建立WebSocket。
- 模拟`SUCCESS`、`FAILED`和`TIMEOUT`。
- 只更新匹配命令。
- 测试断网、离线和气体超限。
- 给停线增加二次确认。
- 显示后端拒绝原因。

## 验收流程

```text
设备在线、工位空闲、气体合格
→ 点击启动水洗
→ 命令已受理
→ MQTT发送
→ 设备ACK
→ 显示执行中
→ 收到SUCCESS
→ 显示水洗完成
```

鸿蒙屏发起并展示，Spring Boot判断，设备执行，MQTT传输，WebSocket返回最终状态。

## 具体实操：连接真实后端并完成启动水洗

### 步骤1：确认局域网地址

在运行Spring Boot的电脑上：

```powershell
ipconfig
```

找出当前IPv4，例如：

```text
192.168.1.100
```

鸿蒙设备和电脑连接同一个路由器，不能在鸿蒙端写`localhost`。

### 步骤2：验证鸿蒙设备能访问后端

在电脑浏览器先访问：

```text
http://192.168.1.100:8088/hdc
```

再检查Windows防火墙是否允许8088。若鸿蒙端仍无法访问，先用同一局域网另一台电脑测试，不要直接改鸿蒙代码。

### 步骤3：实现ApiService查询快照

在鸿蒙`service/ApiService.ets`中配置：

```ts
const BASE_URL: string = 'http://192.168.1.100:8088/hdc'
```

实现：

```text
GET /api/stations/WASH-01/snapshot
```

先让后端返回固定快照，确认HTTP请求通了，再接数据库真实状态。

### 步骤4：打印并解析响应

第一次不要直接绑定页面，先打印：

```text
HTTP状态码
响应文本
code
message
data.stationCode
```

确认字段名称与后端完全一致后，再转换成`StationStatus`模型。

### 步骤5：实现启动请求

请求体：

```json
{
  "stationCode": "WASH-01",
  "deviceCode": "PUMP-01",
  "action": "START",
  "parameters": {
    "durationSeconds": 15
  }
}
```

鸿蒙页面点击后执行：

1. 判断按钮条件。
2. 显示加载。
3. 调用ApiService。
4. 解析`commandId`。
5. 显示`PENDING`。
6. 关闭加载。

### 步骤6：接WebSocket

连接：

```text
ws://192.168.1.100:8088/hdc/api/station/WASH-01
```

连接成功后：

- 发送或等待心跳。
- 记录连接状态。
- 接收`command.status.changed`。
- 接收`alarm.created`。
- 接收`station.snapshot.changed`。

### 步骤7：匹配命令编号

页面保存HTTP返回的`commandId`，收到WebSocket事件后只处理：

```text
message.data.commandId == 当前commandId
```

测试方法：

1. 提交CMD-001。
2. 先推送CMD-999的SUCCESS，页面不能变成功。
3. 再推送CMD-001的EXECUTING，页面显示执行中。
4. 最后推送CMD-001的SUCCESS，页面显示完成。

### 步骤8：接入模拟设备

启动第7课的Node模拟器，让它订阅PUMP-01命令并返回ACK。完整验证：

```text
鸿蒙HTTP请求
→ Spring Boot
→ MQTT命令
→ Node模拟泵
→ MQTT ACK
→ Spring Boot
→ WebSocket
→ 鸿蒙页面
```

每一段都打开日志，确认编号一致。

### 步骤9：测试后端拒绝

依次制造：

```text
气体12.5 ppm
设备OFFLINE
设备RUNNING时重复启动
durationSeconds=9999
```

鸿蒙页面必须显示后端返回的明确原因，不能只显示“网络错误”。

### 步骤10：测试超时和断线

- 模拟器不返回ACK，等待`TIMEOUT`。
- 关闭WebSocket，页面显示数据过期。
- 恢复连接后先请求快照，再接事件。
- HTTP失败时恢复按钮，不允许一直卡在加载状态。

### 步骤11：危险操作确认

给“停止整线”增加确认弹窗，确认文本写明：

```text
即将停止LINE-01，当前正在执行的工序可能暂停，是否继续？
```

取消不发请求，确认后才调用后端；后端仍再次校验权限。

### 本课完成标志

- 鸿蒙屏能使用局域网IP查询后端。
- 能提交控制请求并显示命令编号。
- 能收到匹配命令的实时执行结果。
- 能处理后端拒绝、设备失败、超时、断线和恢复。
- 可以完整展示一条HTTP→MQTT→ACK→WebSocket链路。
